import { Badge } from '@rspress/core/theme';

# 大 Key 发现与解决方案

大 key 是 Redis 性能问题的重要原因之一。当一个 key 的 value 过大时，读取、写入、删除操作都会变慢，甚至导致 Redis 阻塞。

这一讲，我们来聊聊如何发现大 key，以及如何解决大 key 问题。

## 什么是大 Key

大 key 并没有明确定义，通常指：

- String 类型：value 超过 10MB
- List/Set/ZSet/Hash：成员数量超过 10000 个，或总大小超过 10MB

```
大 Key 示例：
- 一个 Hash 包含 100 万个字段
- 一个 List 包含 1000 万个元素
- 一个 String 存储了 100MB 的图片
```

## 大 Key 的危害

### 1. 内存不均

```
Redis Cluster 内存分布：
Node A: 8GB / 10GB  ← 大 key 多的节点
Node B: 5GB / 10GB
Node C: 4GB / 10GB
```

大 key 会导致数据倾斜，部分节点内存使用率过高。

### 2. 请求倾斜

```
QPS 分布：
Node A: 50000 QPS  ← 承载大 key 请求
Node B: 10000 QPS
Node C: 10000 QPS
```

大 key 所在的节点压力过大，成为性能瓶颈。

### 3. 操作阻塞

```
删除一个大 List（1000 万个元素）：
1. 同步删除：可能阻塞 10 秒
2. 读取超时：其他请求等待
3. 从节点复制延迟：主从不一致
```

### 4. 网络带宽

```
读取一个大 String（100MB）：
- 单次请求占用网络带宽 100MB
- 并发几个请求就可能打满网卡
```

## 发现大 Key

### 方法一：redis-cli --bigkeys

```bash
# 扫描大 key
$ redis-cli --bigkeys

# Sample output:
# Scanning 1000000 keys in 0.5015 seconds
#     Big key (string):   1000000 bytes, 1 keys
#     Big key (list):     10000000 items, 1000000 bytes, 1 keys
#     Big key (set):      1000000 members, 50000000 bytes, 1 keys
#     Big key (hash):     1000000 fields, 100000000 bytes, 1 keys
#     Big key (zset):     1000000 members, 100000000 bytes, 1 keys
```

:::warning 注意事项
`--bigkeys` 只扫描每种类型的最大 key，不会扫描所有 key。
:::

### 方法二：SCAN + SIZE 命令

```python
import redis

r = redis.Redis(host='localhost', port=6379)

# 扫描所有 key
for key in r.scan_iter(match='*', count=1000):
    key_type = r.type(key).decode()
    
    if key_type == 'string':
        size = r.strlen(key)
    elif key_type == 'list':
        size = r.llen(key)
    elif key_type == 'set':
        size = r.scard(key)
    elif key_type == 'zset':
        size = r.zcard(key)
    elif key_type == 'hash':
        size = r.hlen(key)
    
    if size > 10000:  # 定义阈值
        print(f"{key}: {key_type} - {size}")
```

### 方法三：内存分析工具

```bash
# 使用 rdb-tools 分析 RDB 文件
$ rdb -c memory --bytes 10485760 dump.rdb > large_keys.csv

# 分析结果
$ cat large_keys.csv | sort -t',' -k4 -rn | head -20
```

### 方法四：Redis 6.2+ MEMORY USAGE

```bash
# 查看单个 key 的内存占用
> MEMORY USAGE user:large:profile
(integer) 52428800  # 50MB

# 查看 key 及其 nested key 的内存占用
> MEMORY USAGE user:large:profile SAMPLES 5
```

## 大 Key 解决方案

### 方案一：拆分

```
原来：
user:profile:10001 -> {field1: v1, field2: v2, ... 100万个字段}

拆分后：
user:profile:10001:basic -> {field1: v1, field2: v2, ...}
user:profile:10001:detail -> {field1: v1, ...}
user:profile:10001:stats -> {field1: v1, ...}
```

```java
// 拆分大 Hash
public class UserProfileCache {
    
    private static final int BATCH_SIZE = 1000;
    
    public void cacheUserProfile(Long userId, Map<String, String> profile) {
        // 按固定数量拆分
        List<Map<String, String>> batches = partition(profile, BATCH_SIZE);
        
        for (int i = 0; i < batches.size(); i++) {
            String key = String.format("user:profile:%d:%d", userId, i);
            redisTemplate.opsForHash().putAll(key, batches.get(i));
            redisTemplate.expire(key, 1, TimeUnit.HOURS);
        }
    }
    
    public Map<String, String> getUserProfile(Long userId) {
        Map<String, String> profile = new HashMap<>();
        
        // 获取所有分片
        int index = 0;
        while (true) {
            String key = String.format("user:profile:%d:%d", userId, index);
            Map<Object, Object> batch = redisTemplate.opsForHash().entries(key);
            
            if (batch.isEmpty()) {
                break;
            }
            
            profile.putAll(batch.entrySet().stream()
                .collect(Collectors.toMap(
                    e -> e.getKey().toString(),
                    e -> e.getValue().toString()
                )));
            index++;
        }
        
        return profile;
    }
}
```

### 方案二：压缩

```java
// 使用压缩存储大 String
public class CompressedCache {
    
    public void set(String key, String value) {
        // 判断是否需要压缩
        if (value.length() > 10240) {  // 10KB
            byte[] compressed = compress(value);
            redisTemplate.opsForValue().set(key, compressed);
            redisTemplate.opsForValue().set(key + ":compressed", "1");
        } else {
            redisTemplate.opsForValue().set(key, value);
        }
    }
    
    public String get(String key) {
        String compressed = (String) redisTemplate.opsForValue().get(key + ":compressed");
        
        if ("1".equals(compressed)) {
            byte[] data = (byte[]) redisTemplate.opsForValue().get(key);
            return decompress(data);
        } else {
            return (String) redisTemplate.opsForValue().get(key);
        }
    }
    
    private byte[] compress(String value) {
        // 使用 GZIP 压缩
        ByteArrayOutputStream baos = new ByteArrayOutputStream();
        try (GZIPOutputStream gzos = new GZIPOutputStream(baos)) {
            gzos.write(value.getBytes(StandardCharsets.UTF_8));
        }
        return baos.toByteArray();
    }
    
    private String decompress(byte[] data) {
        try (GZIPInputStream gzis = new GZIPInputStream(
                new ByteArrayInputStream(data))) {
            return new String(gzis.readAllBytes(), StandardCharsets.UTF_8);
        }
    }
}
```

### 方案三：异步删除

```java
// 使用 UNLINK 代替 DEL
public class AsyncDeleteService {
    
    public void deleteKey(String key) {
        // UNLINK 是异步删除，立即返回
        redisTemplate.unlink(key);
    }
    
    // 或者使用后台线程
    private ExecutorService deleteExecutor = Executors.newFixedThreadPool(4);
    
    public void deleteKeyAsync(String key) {
        deleteExecutor.submit(() -> {
            redisTemplate.delete(key);
        });
    }
}
```

### 方案四：分批操作

```java
// 分批读取大 Hash
public class BatchHashService {
    
    private static final int BATCH_SIZE = 100;
    
    public void processLargeHash(String key) {
        // 使用 HSCAN 分批遍历
        try (Cursor<Map.Entry<Object, Object>> cursor = 
                redisTemplate.opsForHash().scan(key, ScanOptions.scanOptions()
                    .count(BATCH_SIZE).build())) {
            
            while (cursor.hasNext()) {
                Map.Entry<Object, Object> entry = cursor.next();
                processEntry(entry);
            }
        }
    }
    
    // 分批删除
    public void deleteLargeHash(String key) {
        // 分批删除字段
        Long size = redisTemplate.opsForHash().size(key);
        
        int deleted = 0;
        while (deleted < size) {
            // 使用 HDEL 批量删除
            Set<Object> fields = redisTemplate.opsForHash().keys(key);
            
            List<Object> toDelete = fields.stream()
                .limit(BATCH_SIZE)
                .collect(Collectors.toList());
            
            redisTemplate.opsForHash().delete(key, toDelete.toArray());
            deleted += toDelete.size();
        }
    }
}
```

### 方案五：过期策略

```java
// 对大 key 设置过期时间
public class ExpireCache {
    
    public void setWithExpire(String key, String value, long size) {
        long ttl = calculateTTL(size);
        redisTemplate.opsForValue().set(key, value, ttl, TimeUnit.SECONDS);
    }
    
    private long calculateTTL(long size) {
        // size 越大，TTL 越短
        if (size > 10000000) {
            return 3600;       // 1 小时
        } else if (size > 1000000) {
            return 86400;       // 1 天
        } else {
            return 604800;      // 7 天
        }
    }
}
```

## 预防措施

### 1. 写入时检查

```java
public void set(String key, String value) {
    // 检查大小
    if (value.length() > MAX_SIZE) {
        throw new IllegalArgumentException("Value too large");
    }
    
    redisTemplate.opsForValue().set(key, value);
}
```

### 2. 监控告警

```yaml
# Prometheus 告警规则
groups:
  - name: redis_bigkey
    rules:
      - alert: RedisBigKey
        expr: redis_bigstring_keys_bytes > 10485760
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Redis 大 key 告警"
          description: "Key {{ $labels.key }} 超过 10MB"
```

### 3. 定期扫描

```java
@Scheduled(cron = "0 0 2 * * ?")  // 每天凌晨扫描
public void scanLargeKeys() {
    Set<String> largeKeys = new HashSet<>();
    
    for (String key : redisTemplate.scan(ScanOptions.scanOptions()
            .count(1000).build())) {
        
        Long memory = redisTemplate.execute((RedisCallback<Long>) conn -> 
            conn.memoryCommands().memoryUsage(key.getBytes()));
        
        if (memory != null && memory > 10 * 1024 * 1024) {
            largeKeys.add(key);
        }
    }
    
    // 记录日志或发送告警
    log.warn("发现 {} 个大 key: {}", largeKeys.size(), largeKeys);
}
```

## 面试追问方向

- 什么是大 key？有什么危害？
- 如何发现大 key？
- 大 key 如何删除？
- 大 Hash 如何拆分？
- 如何预防大 key？

## 总结

大 key 是 Redis 性能的大敌：

1. **发现大 key**：`--bigkeys`、SCAN、`MEMORY USAGE`
2. **拆分大 key**：按字段数或数据量拆分
3. **异步删除**：使用 UNLINK 代替 DEL
4. **分批操作**：HSCAN、SSCAN 等
5. **预防为主**：写入检查、监控告警、定期扫描

记住：**大 key 问题预防优于治疗**。
