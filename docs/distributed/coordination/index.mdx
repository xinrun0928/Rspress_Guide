# 分布式协调服务：谁来当 Leader？

想象一个场景：

Kafka 集群有 3 台 Broker，其中一台突然宕机了。剩下的两台怎么知道该由谁来接管？Controller 是怎么选出来的？

或者另一个场景：你的微服务有 10 个实例，每次部署新版本，需要更新负载均衡配置。怎么让所有实例的配置同时生效，而不是一个一个改？

**这些问题的答案，都藏在分布式协调服务里。**

分布式协调服务，是分布式系统的"神经中枢"。它负责：
- 服务注册与发现
- 配置管理
- Leader 选举
- 分布式锁

ZooKeeper、etcd、Nacos，是三个最主流的解决方案。

## 模块速览

| 章节 | 篇数 | 核心内容 |
|------|------|----------|
| [ZooKeeper](/distributed/coordination/zookeeper/data-model) | 8 篇 | 数据模型、Watch、Session、Leader 选举、ACL、应用场景 |
| [etcd](/distributed/coordination/etcd/architecture) | 3 篇 | 架构原理、Raft + MVCC、Kubernetes 应用 |
| [Nacos](/distributed/coordination/nacos/architecture) | 5 篇 | 注册中心、配置中心、集群部署、临时/持久实例 |

## 三者对比

| 维度 | ZooKeeper | etcd | Nacos |
|------|-----------|------|-------|
| 一致性协议 | ZAB | Raft | Raft |
| 数据模型 | ZNode 树 | Key-Value | Key-Value |
| Watch 机制 | 一次性触发 | 持续监听 | 持续监听 |
| 生态 | 大数据（Hadoop、Kafka） | Kubernetes | Spring Cloud |
| 适用场景 | 配置管理、分布式锁 | 服务发现、配置中心 | 注册中心、配置中心 |

## 选型决策

```
┌─────────────────────────────────────────────────────────┐
│                    选型决策树                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  你在什么生态？                                          │
│  ├─ Kubernetes → etcd（K8s 深度集成）                  │
│  ├─ Spring Cloud → Nacos（开箱即用）                   │
│  └─ 大数据生态 → ZooKeeper（久经验证）                  │
│                                                         │
│  你需要什么能力？                                        │
│  ├─ 注册中心 + 配置中心 → Nacos                         │
│  ├─ 高可靠配置 + 锁 → ZooKeeper / etcd                 │
│  └─ 轻量级临时配置 → Nacos CP 模式                      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 面试高频问题

### Q1：ZooKeeper 和 etcd 的区别？

> 「从设计目标看，ZooKeeper 是通用的分布式协调服务，etcd 专注于服务发现和配置管理。
> 从协议看，两者都用一致性协议，但 etcd 是纯 Raft，ZooKeeper 是 ZAB。
> 从生态看，etcd 是 Kubernetes 的存储后端，ZooKeeper 是大数据生态的核心组件。」

### Q2：Nacos 和 Eureka 的区别？

> 「Eureka 已经停止维护，Nacos 是更全面的替代方案。
> Nacos 支持注册中心和配置中心，Eureka 只支持注册中心。
> Nacos 支持一致性协议（AP + CP），Eureka 只支持 AP。
> Nacos 有更活跃的社区和更完善的功能。」

## 核心公式

```
分布式协调 = 服务注册发现 + 配置管理 + Leader 选举 + 分布式锁

选型依据：
- Spring Cloud 生态 → Nacos
- Kubernetes 生态 → etcd
- 大数据生态 → ZooKeeper
```
