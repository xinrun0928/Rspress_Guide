# 可扩展性设计：让系统「长大」

你的系统从 1 万用户增长到 1000 万用户。

系统还撑得住吗？

可扩展性，就是让系统能够「长大」的能力。

## 一、可扩展性的核心问题

### 1.1 什么是可扩展性

```
┌─────────────────────────────────────────────────────┐
│                   可扩展性定义                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│  垂直扩展（Scale Up）                                │
│  ├── 提升单机配置                                   │
│  ├── CPU 升级、内存扩大、磁盘换 SSD                 │
│  └── 优点：简单                                     │
│  └── 缺点：有上限                                  │
│                                                     │
│  水平扩展（Scale Out）                               │
│  ├── 增加机器数量                                   │
│  ├── 多台机器协同工作                               │
│  └── 优点：理论上无上限                            │
│  └── 缺点：复杂度高                                │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 1.2 可扩展性的挑战

```
可扩展性面临的挑战：

1. 无状态化
   - 服务不能保存用户状态
   - 状态必须存储到外部

2. 数据分片
   - 数据量太大，单机存不下
   - 需要分片存储

3. 服务拆分
   - 单体应用太大
   - 需要拆分成多个服务

4. 负载均衡
   - 多台机器如何分配请求
   - 如何检测机器故障

5. 分布式事务
   - 数据分片后的事务问题
   - 如何保证一致性
```

## 二、无状态化设计

### 2.1 什么是无状态服务

```java title="src/main/java/StatelessService.java"
public class StatelessService {

    // 有状态服务的问题：
    // - 用户 A 的请求被路由到服务器 1
    // - 用户 A 的 Session 存在服务器 1
    // - 如果请求被路由到服务器 2，找不到 Session

    // 无状态服务的设计：
    // - 服务不存储任何用户状态
    // - 所有状态存储在外部（Redis、数据库）
    // - 任何请求可以被路由到任何服务器

    // 无状态服务的优势：
    // - 任何请求可以路由到任何服务器
    // - 服务器可以随时扩容/缩容
    // - 故障时可以快速切换
}

// 错误示例：有状态服务
public class StatefulOrderService {
    private Map<Long, Order> localCache = new HashMap<>(); // 本地缓存

    public Order getOrder(Long orderId) {
        // 每次都查本地缓存
        return localCache.get(orderId);
    }
}

// 正确示例：无状态服务
public class StatelessOrderService {
    private RedisTemplate<String, Object> redisTemplate;

    public Order getOrder(Long orderId) {
        // 状态存储在 Redis
        String key = "order:" + orderId;
        return (Order) redisTemplate.opsForValue().get(key);
    }
}
```

### 2.2 Session 管理

```java title="src/main/java/SessionManagement.java"
public class SessionManagement {

    // Session 外置方案

    // 方案一：Redis Session
    // Spring Session + Redis
    /*
    @Configuration
    public class SessionConfig {
        @Bean
        public RedisIndexedHttpSessionRepository sessionRepository() {
            RedisIndexedHttpSessionRepository repo = new RedisIndexedHttpSessionRepository();
            repo.setRedisTemplate(redisTemplate);
            repo.setDefaultSerializer(new JacksonRedisSerializer<>(Object.class));
            return repo;
        }
    }
    */

    // 方案二：JWT Token
    // 优点：服务端无状态
    // 缺点：Token 一旦签发无法撤销

    @Autowired
    private StringRedisTemplate redisTemplate;

    // 生成 Session
    public String createSession(Long userId) {
        String sessionId = UUID.randomUUID().toString();
        String key = "session:" + sessionId;

        Map<String, String> sessionData = new HashMap<>();
        sessionData.put("userId", userId.toString());
        sessionData.put("loginTime", String.valueOf(System.currentTimeMillis()));

        redisTemplate.opsForHash().putAll(key, sessionData);
        redisTemplate.expire(key, 30, TimeUnit.MINUTES);

        return sessionId;
    }

    // 验证 Session
    public Long validateSession(String sessionId) {
        String key = "session:" + sessionId;
        String userId = (String) redisTemplate.opsForHash().get(key, "userId");

        if (userId == null) {
            return null;
        }

        // 续期
        redisTemplate.expire(key, 30, TimeUnit.MINUTES);

        return Long.parseLong(userId);
    }
}
```

## 三、数据分片

### 3.1 分片策略

```java title="src/main/java/ShardingStrategy.java"
public class ShardingStrategy {

    // 常见分片策略

    // 1. 按 ID 哈希分片
    // 优点：数据分布均匀
    // 缺点：按范围查询困难
    public int hashSharding(long id, int shardCount) {
        return (int) (id % shardCount);
    }

    // 2. 按时间分片
    // 优点：按时间范围查询快
    // 缺点：可能出现热点
    public String timeSharding(LocalDateTime time) {
        return String.format("%d%02d", time.getYear(), time.getMonthValue());
    }

    // 3. 按用户 ID 分片
    // 优点：同一用户的数据在同一分片
    // 缺点：热点用户数据不均
    public int userSharding(long userId, int shardCount) {
        return (int) (userId % shardCount);
    }

    // 4. 一致性哈希分片
    // 优点：扩缩容时迁移数据少
    // 缺点：实现复杂
    public int consistentHashSharding(long id, int virtualNodes) {
        // 使用 MurmurHash 计算哈希
        long hash = murmurHash(id);

        // 一致性哈希环上定位
        return (int) (Math.abs(hash % virtualNodes));
    }
}
```

### 3.2 分片中间件

```java title="src/main/java/ShardingSphereConfig.java"
/*
# ShardingSphere 配置示例

schemaName: sharding_db

dataSources:
  ds_0:
    url: jdbc:mysql://localhost:3306/ds_0
    username: root
    password:
  ds_1:
    url: jdbc:mysql://localhost:3306/ds_1
    username: root
    password:

rules:
  - !SHARDING
    tables:
      t_order:
        actualDataNodes: ds_${0..1}.t_order_${0..15}
        databaseStrategy:
          standard:
            shardingColumn: user_id
            shardingAlgorithmName: database_inline
        tableStrategy:
          standard:
            shardingColumn: order_id
            shardingAlgorithmName: table_inline

    bindingTables:
      - t_order,t_order_item

    defaultDatabaseStrategy:
      standard:
        shardingColumn: user_id
        shardingAlgorithmName: database_inline

    shardingAlgorithms:
      database_inline:
        type: INLINE
        props:
          algorithm-expression: ds_${user_id % 2}
      table_inline:
        type: INLINE
        props:
          algorithm-expression: t_order_${order_id % 16}
*/
```

### 3.3 分片后的查询

```java title="src/main/java/ShardedQueryService.java"
public class ShardedQueryService {

    // 分片后跨分片查询

    // 查询所有订单（跨分片聚合）
    public List<Order> getAllOrders(Long userId) {
        List<Order> allOrders = new ArrayList<>();
        int shardCount = 16;

        // 并行查询所有分片
        List<Future<List<Order>>> futures = new ArrayList<>();

        for (int i = 0; i < shardCount; i++) {
            Future<List<Order>> future = CompletableFuture.supplyAsync(() -> {
                return queryOrdersFromShard(userId, i);
            });
            futures.add(future);
        }

        // 等待所有结果
        for (Future<List<Order>> future : futures) {
            try {
                allOrders.addAll(future.get());
            } catch (Exception e) {
                // 处理异常
            }
        }

        // 排序
        allOrders.sort((a, b) -> b.getCreateTime().compareTo(a.getCreateTime()));

        return allOrders;
    }

    private List<Order> queryOrdersFromShard(Long userId, int shardIndex) {
        // 查询指定分片
        return jdbcTemplate.query(
            "SELECT * FROM t_order WHERE user_id = ? AND shard_id = ?",
            new Object[]{userId, shardIndex},
            new BeanPropertyRowMapper<>(Order.class)
        );
    }
}
```

## 四、服务拆分

### 4.1 拆分策略

```
┌─────────────────────────────────────────────────────┐
│                   微服务拆分原则                       │
├─────────────────────────────────────────────────────┤
│                                                     │
│  1. 按业务能力拆分                                   │
│  ├── 用户服务：注册、登录、个人信息                  │
│  ├── 订单服务：创建订单、取消订单                   │
│  ├── 支付服务：发起支付、退款                       │
│  └── 商品服务：商品详情、库存                       │
│                                                     │
│  2. 按领域模型拆分                                   │
│  ├── 用户域：用户、角色、权限                       │
│  ├── 订单域：订单、订单项、物流                     │
│  └── 支付域：支付、账户、流水                       │
│                                                     │
│  3. 拆分粒度原则                                     │
│  ├── 避免过度拆分（微服务 → 迷你服务）             │
│  ├── 优先按业务边界拆分                             │
│  └── 避免过多的跨服务调用                          │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 4.2 服务间通信

```java title="src/main/java/ServiceCommunication.java"
public class ServiceCommunication {

    @Autowired
    private RestTemplate restTemplate;

    @Autowired
    private LoadBalancer loadBalancer;

    // 服务调用
    public User getUserWithOrder(Long userId, Long orderId) {
        // 调用用户服务
        String userServiceUrl = loadBalancer.choose("user-service");
        User user = restTemplate.getForObject(
            userServiceUrl + "/users/" + userId,
            User.class
        );

        // 调用订单服务
        String orderServiceUrl = loadBalancer.choose("order-service");
        Order order = restTemplate.getForObject(
            orderServiceUrl + "/orders/" + orderId,
            Order.class
        );

        user.setOrder(order);
        return user;
    }

    // 异步调用
    public CompletableFuture<User> getUserAsync(Long userId) {
        return CompletableFuture.supplyAsync(() -> {
            String userServiceUrl = loadBalancer.choose("user-service");
            return restTemplate.getForObject(
                userServiceUrl + "/users/" + userId,
                User.class
            );
        });
    }
}
```

## 五、负载均衡

### 5.1 负载均衡策略

```java title="src/main/java/LoadBalancingStrategy.java"
public class LoadBalancingStrategy {

    // 负载均衡算法

    // 1. 轮询（Round Robin）
    public String roundRobin(List<String> servers, AtomicInteger counter) {
        int index = counter.incrementAndGet() % servers.size();
        return servers.get(index);
    }

    // 2. 加权轮询（Weighted Round Robin）
    public String weightedRoundRobin(List<ServerWeight> servers) {
        int totalWeight = servers.stream()
            .mapToInt(ServerWeight::getWeight)
            .sum();

        int random = ThreadLocalRandom.current().nextInt(totalWeight);

        int sum = 0;
        for (ServerWeight sw : servers) {
            sum += sw.getWeight();
            if (random < sum) {
                return sw.getServer();
            }
        }

        return servers.get(0).getServer();
    }

    // 3. 最少连接（Least Connections）
    public String leastConnections(Map<String, AtomicInteger> connections) {
        return connections.entrySet().stream()
            .min(Map.Entry.comparingByValue())
            .map(Map.Entry::getKey)
            .orElse(null);
    }

    // 4. 一致性哈希
    public String consistentHash(String key,
                                 TreeMap<Long, String> ring) {
        long hash = murmurHash(key);

        // 找到顺时针方向的第一个节点
        Map.Entry<Long, String> entry = ring.ceilingEntry(hash);
        if (entry == null) {
            entry = ring.firstEntry();
        }

        return entry.getValue();
    }
}
```

### 5.2 健康检查

```java title="src/main/java/HealthChecker.java"
public class HealthChecker {

    // 健康检查

    // 主动检查：定时检测服务健康状态
    @Scheduled(fixedRate = 5000)
    public void checkHealth() {
        for (Server server : servers) {
            try {
                // 发送 HTTP 请求检测
                ResponseEntity<String> response = restTemplate.getForEntity(
                    server.getUrl() + "/health",
                    String.class
                );

                if (response.getStatusCode() == HttpStatus.OK) {
                    server.setHealthy(true);
                    server.setFailCount(0);
                } else {
                    server.setHealthy(false);
                }
            } catch (Exception e) {
                server.setHealthy(false);
                server.incrementFailCount();

                // 失败次数过多，标记为不可用
                if (server.getFailCount() > 3) {
                    removeFromPool(server);
                }
            }
        }
    }

    // 被动检查：请求失败时标记
    public void onRequestFailed(Server server) {
        server.incrementFailCount();

        if (server.getFailCount() > 3) {
            server.setHealthy(false);
        }
    }
}
```

## 六、扩展性案例

### 6.1 秒杀系统扩展

```java title="src/main/java/SecKillScalability.java"
public class SecKillScalability {

    // 秒杀系统的扩展方案

    // 1. 动静分离
    public void staticDynamicSeparation() {
        // 静态资源（CSS、JS、图片）放 CDN
        // 动态请求（库存、订单）走服务器

        // 秒杀页面缓存在用户浏览器
        // 设置较长的缓存时间
    }

    // 2. 读多写少：多级缓存
    public void multiLevelCache() {
        // L1：本地缓存（Guava/Caffeine）
        // L2：Redis 缓存
        // L3：数据库

        // 读流程：
        // L1 → L2 → L3
    }

    // 3. 写请求削峰
    public void writeDegrade() {
        // 写请求进入消息队列
        // 异步处理订单

        // 用户先得到"排队中"的响应
        // 稍后收到抢单结果
    }

    // 4. 分桶库存
    public void bucketInventory() {
        // 10000 件商品，分成 100 个桶
        // 每个桶 100 件
        // 用户请求被路由到不同桶
    }
}
```

### 6.2 数据库扩展

```java title="src/main/java/DatabaseScalability.java"
public class DatabaseScalability {

    // 数据库扩展路径

    // 第一步：读写分离
    public void readWriteSplit() {
        // 主库：写
        // 从库：读

        // 数据同步：主从复制
    }

    // 第二步：分库分表
    public void sharding() {
        // 按用户 ID 分库
        // 按订单 ID 分表

        // 分片键选择很重要
    }

    // 第三步：冷热分离
    public void hotColdSeparation() {
        // 热数据：Redis + SSD
        // 冷数据：HDD + 归档

        // 定期迁移冷数据
    }

    // 第四步：多活
    public void multiActive() {
        // 多个机房同时对外服务
        // 数据同步

        // 成本高，大公司才用
    }
}
```

## 七、面试高频问题

### Q1：如何设计一个可扩展的系统？

```
设计要点：

1. 无状态化
   - 服务不保存本地状态
   - 状态外置到 Redis/数据库

2. 水平扩展
   - 服务无状态后可以随时扩容
   - 负载均衡分发请求

3. 数据分片
   - 数据量大时分片存储
   - 按业务选择分片键

4. 服务拆分
   - 按业务边界拆分
   - 避免过度拆分

5. 异步化
   - 非核心路径异步化
   - 削峰填谷
```

### Q2：如何选择分片键？

```
选择原则：

1. 查询维度
   - 经常按这个字段查询
   - 优先放在分片键

2. 数据分布
   - 分布要均匀
   - 避免热点

3. 业务场景
   - 订单按用户 ID 分片
   - 帖子按内容 ID 分片

常见选择：
- 用户 ID
- 订单 ID
- 时间
```

### Q3：如何平滑扩容？

```
平滑扩容方案：

1. 迁移前准备
   - 新集群部署
   - 数据同步

2. 双写策略
   - 同时写入新旧集群
   - 校验数据一致性

3. 灰度切换
   - 先切换小部分流量
   - 逐步扩大

4. 旧集群下线
   - 确认无误后下线
```

## 八、总结

```
┌─────────────────────────────────────────────────────┐
│               可扩展性设计核心知识点                     │
├─────────────────────────────────────────────────────┤
│                                                     │
│  核心思想                                            │
│  ├── 无状态化：服务可随时扩容                        │
│  ├── 数据分片：突破单机存储限制                      │
│  ├── 服务拆分：按业务边界拆分                       │
│  └── 异步化：削峰填谷                              │
│                                                     │
│  扩展路径                                            │
│  ├── 读写分离                                      │
│  ├── 分库分表                                      │
│  ├── 缓存加速                                      │
│  └── 微服务化                                      │
│                                                     │
│  关键设计                                            │
│  ├── 选择合适的分片键                               │
│  ├── 保证数据一致性                                 │
│  └── 平滑扩容方案                                 │
│                                                     │
└─────────────────────────────────────────────────────┘
```

**面试加分点**：
- 能解释无状态化的意义和实现
- 能说明数据分片的策略和注意事项
- 能设计服务拆分的方案
- 能分析系统的扩展路径
