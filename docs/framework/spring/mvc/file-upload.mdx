# 文件上传与下载实现

import { Badge } from '@rspress/core/theme';

<Badge text="文件上传" type="tip" /> <Badge text="MultipartResolver" type="info" /> <Badge text="实战应用" type="warning" />

Spring MVC 是如何处理文件上传的？这一节，我们来学习文件上传和下载的实现。

---

## 一、配置 MultipartResolver

```java
@Configuration
public class WebMvcConfig {
    @Bean
    public MultipartResolver multipartResolver() {
        CommonsMultipartResolver resolver = new CommonsMultipartResolver();
        resolver.setMaxUploadSize(10 * 1024 * 1024);  // 10MB
        resolver.setDefaultEncoding("UTF-8");
        return resolver;
    }
}
```

Spring Boot 配置：

```properties
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB
```

---

## 二、单文件上传

```java
@PostMapping("/upload")
public Result upload(@RequestParam("file") MultipartFile file) {
    if (file.isEmpty()) {
        return Result.error("文件不能为空");
    }
    String filename = file.getOriginalFilename();
    File dest = new File("/uploads/" + filename);
    file.transferTo(dest);
    return Result.success("/uploads/" + filename);
}
```

---

## 三、多文件上传

```java
@PostMapping("/upload/multiple")
public Result uploadMultiple(@RequestParam("files") MultipartFile[] files) {
    List<String> urls = new ArrayList<>();
    for (MultipartFile file : files) {
        if (!file.isEmpty()) {
            String url = saveFile(file);
            urls.add(url);
        }
    }
    return Result.success(urls);
}
```

---

## 四、文件下载

```java
@GetMapping("/download/{filename}")
public ResponseEntity<Resource> download(@PathVariable String filename) {
    Resource resource = new FileSystemResource("/uploads/" + filename);
    return ResponseEntity.ok()
        .header(HttpHeaders.CONTENT_DISPOSITION,
            "attachment; filename=" + filename)
        .body(resource);
}
```

---

## 五、面试追问方向

**「Spring MVC 如何处理文件上传？」**

通过 `MultipartResolver` 解析 multipart 请求，将上传的文件封装为 `MultipartFile`。

**「文件上传大小限制如何配置？」**

Spring Boot 通过 `spring.servlet.multipart.max-file-size` 配置。
