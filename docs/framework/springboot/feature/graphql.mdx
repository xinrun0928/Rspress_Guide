import { Badge } from '@rspress/core/theme';

# Spring Boot 整合 GraphQL

传统的 RESTful API 存在一个问题：Over-fetching（获取过多数据）和 Under-fetching（需要多次请求）。GraphQL 是一种用于 API 的查询语言，允许客户端精确指定需要的数据。

这一节，我们来学习 Spring Boot 如何整合 GraphQL。

## 一、GraphQL 基础

### 什么是 GraphQL？

GraphQL 是一种 API 查询语言，由 Facebook 在 2012 年开发，2015 年开源。与 RESTful API 不同，GraphQL 只返回一个端点，客户端指定需要哪些字段。

### GraphQL vs RESTful

| 对比项 | GraphQL | RESTful |
|---|---|---|
| 数据获取 | 一次请求获取所需数据 | 多次请求获取多个资源 |
| 端点 | 单个端点 | 多个端点 |
| 版本控制 | 不需要版本控制 | 需要版本控制（v1, v2） |
| 字段控制 | 客户端决定 | 服务器决定 |
| 缓存 | 较复杂 | 简单（URL 缓存） |
| 学习曲线 | 较陡 | 平缓 |

### GraphQL 操作类型

| 操作类型 | 说明 | 关键字 |
|---|---|---|
| Query | 查询数据 | query |
| Mutation | 修改数据 | mutation |
| Subscription | 实时订阅 | subscription |

## 二、Spring Boot GraphQL 依赖

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-graphql</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-graphql</artifactId>
</dependency>
<!-- 启用 GraphQL UI -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-graphiql</artifactId>
</dependency>
```

## 三、定义 Schema

### Schema 文件

在 `src/main/resources/graphql/` 目录下创建 Schema 文件：

```graphql
# schema.graphqls
type Query {
    users: [User!]!
    user(id: ID!): User
    posts: [Post!]!
    post(id: ID!): Post
}

type Mutation {
    createUser(input: CreateUserInput!): User!
    updateUser(id: ID!, input: UpdateUserInput!): User
    deleteUser(id: ID!): Boolean!
    createPost(input: CreatePostInput!): Post!
}

type Subscription {
    userCreated: User!
    postCreated: Post!
}

type User {
    id: ID!
    name: String!
    email: String!
    age: Int
    posts: [Post!]!
    createdAt: DateTime!
}

type Post {
    id: ID!
    title: String!
    content: String!
    author: User!
    comments: [Comment!]!
    createdAt: DateTime!
}

type Comment {
    id: ID!
    content: String!
    author: User!
    createdAt: DateTime!
}

input CreateUserInput {
    name: String!
    email: String!
    age: Int
}

input UpdateUserInput {
    name: String
    email: String
    age: Int
}

input CreatePostInput {
    title: String!
    content: String!
    authorId: ID!
}

scalar DateTime
```

## 四、定义类型

### POJO 类

```java
public class User {
    private Long id;
    private String name;
    private String email;
    private Integer age;
    private List<Post> posts;
    private LocalDateTime createdAt;

    // 构造函数、Getter/Setter
}
```

### Input 类

```java
public class CreateUserInput {
    private String name;
    private String email;
    private Integer age;

    // 构造函数、Getter/Setter
}
```

## 五、定义 Controller

### Query Controller

```java
@Controller
public class UserGraphQLController {

    @Autowired
    private UserService userService;

    @QueryMapping
    public List<User> users() {
        return userService.findAll();
    }

    @QueryMapping
    public User user(@Argument Long id) {
        return userService.findById(id);
    }
}
```

### Mutation Controller

```java
@Controller
public class UserMutationController {

    @Autowired
    private UserService userService;

    @MutationMapping
    public User createUser(@Argument CreateUserInput input) {
        return userService.create(input);
    }

    @MutationMapping
    public User updateUser(@Argument Long id, @Argument UpdateUserInput input) {
        return userService.update(id, input);
    }

    @MutationMapping
    public boolean deleteUser(@Argument Long id) {
        return userService.delete(id);
    }
}
```

### Subscription Controller

```java
@Controller
public class UserSubscriptionController {

    private final Sinks.Many<User> userSink = Sinks.many().multicast().onBackpressureBuffer();

    @MutationMapping
    public User createUser(@Argument CreateUserInput input) {
        User user = userService.create(input);
        userSink.tryEmitNext(user);  // 发布事件
        return user;
    }

    @SubscriptionMapping
    public Flux<User> userCreated() {
        return userSink.asFlux();
    }
}
```

## 六、关联数据加载

### DataLoader

```java
@Component
public class UserDataLoader implements BatchLoader<Long, User> {

    @Override
    public CompletionStage<List<User>> load(List<Long> ids) {
        return CompletableFuture.supplyAsync(() -> userService.findByIds(ids));
    }
}
```

### 在 Controller 中使用

```java
@Controller
public class PostGraphQLController {

    @Autowired
    private PostService postService;

    @QueryMapping
    public List<Post> posts() {
        return postService.findAll();
    }

    @SchemaMapping(typeName = "Post", field = "author")
    public CompletableFuture<User> getAuthor(Post post, DataLoader<Long, User> dataLoader) {
        return dataLoader.load(post.getAuthorId());
    }
}
```

## 七、配置

### application.yml

```yaml
spring:
  graphql:
    path: /graphql
    graphiql:
      enabled: true
    schema:
      locations: classpath:graphql/
      fileExtensions: .graphqls
    execution:
      max-query-depth: 10
      max-complexity: 1000
```

### 启用 GraphiQL UI

启动应用后访问 `http://localhost:8080/graphiql`。

## 八、异常处理

### GraphQLError

```java
@ControllerAdvice
public class GraphQLExceptionHandler {

    @GraphQLExceptionHandler
    public GraphQLError handleUserNotFound(UserNotFoundException ex) {
        return GraphqlErrorBuilder.newError()
            .message("用户不存在")
            .extension("code", "USER_NOT_FOUND")
            .extension("userId", ex.getUserId())
            .build();
    }
}
```

### 客户端错误处理

```java
@ControllerAdvice
public class ValidationExceptionHandler {

    @GraphQLExceptionHandler
    public GraphQLError handleValidation(ValidationException ex) {
        return GraphqlErrorBuilder.newError()
            .message("参数校验失败")
            .extensions(Map.of(
                "code", "VALIDATION_ERROR",
                "errors", ex.getErrors()
            ))
            .build();
    }
}
```

## 九、查询示例

### GraphiQL 查询

```graphql
query GetUserWithPosts($userId: ID!) {
    user(id: $userId) {
        id
        name
        email
        posts {
            id
            title
            content
        }
    }
}

# 变量
{
    "userId": 1
}
```

```graphql
mutation CreateUser($input: CreateUserInput!) {
    createUser(input: $input) {
        id
        name
        email
    }
}

# 变量
{
    "input": {
        "name": "张三",
        "email": "zhang@example.com",
        "age": 25
    }
}
```

```graphql
subscription OnUserCreated {
    userCreated {
        id
        name
        email
    }
}
```

## 十、常见问题

### Q1：如何处理 N+1 问题？

使用 DataLoader 批量加载：

```java
@Component
public class PostDataLoader implements BatchLoader<Long, List<Post>> {

    @Override
    public CompletionStage<List<List<Post>>> load(List<Long> userIds) {
        return CompletableFuture.supplyAsync(() -> postService.findByAuthorIds(userIds));
    }
}
```

### Q2：如何保护 GraphQL 端点？

使用 Spring Security：

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/graphql", "/graphiql").authenticated()
            )
            .httpBasic();
        return http.build();
    }
}
```

### Q3：GraphQL 如何做分页？

```graphql
type Query {
    users(page: Int!, size: Int!): UserConnection!
}

type UserConnection {
    edges: [UserEdge!]!
    pageInfo: PageInfo!
    totalCount: Int!
}

type UserEdge {
    node: User!
    cursor: String!
}

type PageInfo {
    hasNextPage: Boolean!
    hasPreviousPage: Boolean!
    startCursor: String
    endCursor: String
}
```

## 十一、总结

Spring Boot GraphQL 开发流程：

```
┌─────────────────────────────────────────────────────────────────┐
│                    GraphQL 开发流程                               │
├─────────────────────────────────────────────────────────────────┤
│  1. 定义 Schema（.graphqls）                                    │
│     └── type Query/Mutation/Subscription                        │
│                                                                  │
│  2. 实现 Controller                                              │
│     └── @QueryMapping / @MutationMapping / @SubscriptionMapping  │
│                                                                  │
│  3. 配置 DataLoader（N+1 问题）                                │
│                                                                  │
│  4. 异常处理                                                     │
│     └── @GraphQLExceptionHandler                                │
│                                                                  │
│  5. 测试（GraphiQL UI）                                         │
└─────────────────────────────────────────────────────────────────┘
```

> 至此，Spring Boot 常用功能部分完成。所有文档已全部创建完成。
