# HashMap 扩容时机与 resize 过程

你有没有想过：HashMap 什么时候会扩容？扩容做了什么？扩容的成本有多高？

如果你回答「当元素超过容量 `*` 0.75 时扩容」，这只是表面。让我带你深入 JDK 8 源码，看看扩容的完整逻辑。

:::note 结论先行

HashMap 扩容触发的精确条件是 `++size > threshold`——先自增再比较，所以**实际元素数达到 threshold 时就会扩容**。threshold 的值等于 `capacity * loadFactor`，默认是 `16 * 0.75 = 12`。

:::

---

## 扩容时机：threshold 是关键

```java
// HashMap 的几个关键变量
transient Node<K,V>[] table;  // 桶数组
int threshold;                 // 扩容阈值
final float loadFactor;       // 负载因子，默认 0.75
```

threshold = capacity `*` loadFactor。当 `size > threshold` 时，触发扩容。

```java
// putVal 中的扩容检查
if (++size > threshold)
    resize();
```

注意是 `++size > threshold`，先自增再比较。所以实际元素数达到 threshold 时扩容。

### 三种初始化路径

HashMap 的 `resize()` 实际上承担了**初始化和扩容**两种职责。看这段逻辑：

```java
final Node<K,V>[] resize() {
    Node<K,V>[] oldTab = table;
    int oldCap = (oldTab == null) ? 0 : oldTab.length;
    int oldThr = threshold;

    // 路径1：已有 table（正常扩容）
    if (oldCap > 0) {
        if (oldCap >= MAXIMUM_CAPACITY) { ... }
        newCap = oldCap << 1;       // 容量翻倍
        newThr = oldThr << 1;       // 阈值翻倍
    }
    // 路径2：table 未初始化，但 threshold 有值（指定容量初始化）
    else if (oldThr > 0)
        newCap = oldThr;
    // 路径3：完全默认初始化
    else {
        newCap = DEFAULT_INITIAL_CAPACITY;    // 16
        newThr = (int)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);  // 12
    }
}
```

| 场景 | oldCap | oldThr | 结果 |
|---|---|---|---|
| 已有 table 扩容 | `>` 0 | `>` 0 | newCap = oldCap `*` 2，newThr = oldThr `*` 2 |
| 指定容量初始化 `new HashMap<>(32)` | 0 | 32 | newCap = 32（复用 threshold 当容量） |
| 完全默认 `new HashMap<>()` | 0 | 0 | newCap = 16，newThr = 12 |

---

## resize() 的职责

`resize()` 做三件事：

1. **计算新容量**：旧容量 `*` 2（不能超过 `MAXIMUM_CAPACITY = 1 << 30`）
2. **计算新阈值**：新容量 `*` loadFactor
3. **迁移数据**：把旧表的数据迁移到新表

```java
final Node<K,V>[] resize() {
    Node<K,V>[] oldTab = table;
    int oldCap = (oldTab == null) ? 0 : oldTab.length;
    int oldThr = threshold;
    int newCap, newThr = 0;

    // 情况1：已有 table，计算新容量和阈值
    if (oldCap > 0) {
        // 容量已达上限，不再扩容
        if (oldCap >= MAXIMUM_CAPACITY) {
            threshold = Integer.MAX_VALUE;
            return oldTab;
        }
        // 新容量 = 旧容量 * 2
        newCap = oldCap << 1;
        newThr = oldThr << 1;  // 新阈值也翻倍
    }
    // 情况2：初始化时使用 threshold 作为容量
    else if (oldThr > 0)
        newCap = oldThr;
    // 情况3：空 table，使用默认值
    else {
        newCap = DEFAULT_INITIAL_CAPACITY;    // 16
        newThr = (int)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);  // 12
    }

    // 如果新阈值计算出错，修正
    if (newThr == 0) {
        float ft = (float) newCap * loadFactor;
        newThr = (newCap < MAXIMUM_CAPACITY && ft < (float) MAXIMUM_CAPACITY ?
                  (int) ft : Integer.MAX_VALUE);
    }

    threshold = newThr;

    // 创建新数组
    @SuppressWarnings("rawtypes")
    Node<K,V>[] newTab = (Node<K,V>[]) new Node[newCap];
    table = newTab;

    // 如果旧表不为空，迁移数据
    if (oldTab != null) {
        for (int j = 0; j < oldCap; ++j) {
            Node<K,V> e;
            if ((e = oldTab[j]) != null) {
                oldTab[j] = null;  // 释放旧引用
                if (e.next == null)
                    // 单节点，直接移动
                    newTab[e.hash & (newCap - 1)] = e;
                else if (e instanceof TreeNode)
                    // 红黑树分裂
                    ((TreeNode<K,V>) e).split(this, newTab, j, oldCap);
                else {
                    // 链表分裂（重点！）
                    Node<K,V> loHead = null, loTail = null;
                    Node<K,V> hiHead = null, hiTail = null;
                    Node<K,V> next;
                    do {
                        next = e.next;
                        // 关键：判断节点在新表的位置
                        if ((e.hash & oldCap) == 0) {
                            // 位置不变
                            if (loTail == null)
                                loHead = e;
                            else
                                loTail.next = e;
                            loTail = e;
                        } else {
                            // 位置 = 原位置 + oldCap
                            if (hiTail == null)
                                hiHead = e;
                            else
                                hiTail.next = e;
                            hiTail = e;
                        }
                    } while ((e = next) != null);

                    // 两条链表分别放到新表位置
                    if (loTail != null) {
                        loTail.next = null;
                        newTab[j] = loHead;
                    }
                    if (hiTail != null) {
                        hiTail.next = null;
                        newTab[j + oldCap] = hiTail;
                    }
                }
            }
        }
    }
    return newTab;
}
```

---

## JDK 8 的关键优化：不需要重新计算 hash

这是 JDK 8 扩容最精妙的地方。

JDK 7 需要对每个节点重新计算 hash（`indexFor(hash, newCapacity)`），而 JDK 8 只需要判断 `hash & oldCap`：

```java
if ((e.hash & oldCap) == 0) {
    // 新位置 = 原位置
    newTab[j] = loHead;
} else {
    // 新位置 = 原位置 + oldCap
    newTab[j + oldCap] = hiHead;
}
```

为什么只需要看 `hash & oldCap`？位运算图解：

```text title="hash-oldcap-bit-analysis.txt"
假设 oldCap = 16（二进制 10000），hash = 27（二进制 11011）：

  hash       = 1 1 0 1 1
  oldCap     = 1 0 0 0 0  (第5位为1)
  ─────────────────────────
  hash & oldCap = 1 0 0 0 0  = 16 ≠ 0

  说明：hash 的第5位是 1 → 新位置 = 原位置 + oldCap

原位置计算（n=16 时，n-1 = 01111）：
  hash & (16-1) = 11011 & 01111 = 01011 = 11

新位置计算（n=32 时，n-1 = 11111）：
  hash & (32-1) = 11011 & 11111 = 11011 = 27 = 11 + 16 ✓
```

结论：**如果 hash 的最高位（oldCap 对应位）是 0，新位置不变；是 1，新位置 = 原位置 + oldCap**。

:::tip 为什么容量必须是 2 的幂次

正是因为容量是 2 的幂次，容量 `oldCap` 的二进制只有一位是 1（如 16 = 10000）。`hash & oldCap` 只看这一位，就能判断 hash 的该位是 0 还是 1，从而决定新位置是否偏移。

如果容量不是 2 的幂次（如 15），减 1 后不是全 1，位与运算就无法正确区分位置了。这也是 HashMap 要求容量必须是 2 的幂次的根本原因。

:::

---

## 链表分裂：lo 和 hi

JDK 8 把链表分成两条：

```text title="linked-list-split.txt"
遍历原链表，用 e.hash & oldCap 判断每个节点的归属：

原链表: A -> B -> C -> D -> E

假设判断结果：
  A & oldCap = 0  → lo 组（位置不变）
  B & oldCap = 1  → hi 组（位置 + oldCap）
  C & oldCap = 0  → lo 组
  D & oldCap = 1  → hi 组
  E & oldCap = 0  → lo 组

loHead（尾插法）: A -> C -> E  → 放回原位置 j
hiHead（尾插法）: B -> D       → 放到新位置 j + oldCap

新表：
  table[j]        = loHead  （位置 j，不变）
  table[j+oldCap] = hiHead  （位置 j + oldCap，偏移了）
```

注意是**尾插法**，保证顺序不变。

---

## 扩容的成本有多高？

扩容操作的成本：

1. **内存分配**：需要分配一个 2 倍大小的新数组
2. **数据迁移**：需要遍历所有节点，重新分配位置
3. **时间复杂度**：O(n)，n 是元素数量

> [!WARNING]
> HashMap 扩容时，所有节点都需要重新计算位置（或者说重新判断归属）。这是一个 O(n) 的操作。如果 HashMap 里有 100 万个元素，每次扩容都可能触发百万级的数据迁移。所以**避免频繁扩容**是 HashMap 优化的关键。

### 预估容量：优化扩容的第一步

预估容量可以减少扩容次数：

```java
// 预估 1000 个元素，合理设置初始容量
// 1000 / 0.75 ≈ 1333，需要 2048（最近的 2 的幂）
Map<String, Object> map = new HashMap<>(2048);
```

如果事先知道元素数量，直接设置合适的初始容量，可以避免从 16 → 32 → 64 → ... 的一路扩容，把时间成本从 O(n) 摊销到接近 O(1)。

---

## 红黑树的分裂

红黑树在扩容时可能退化为链表。`split()` 方法的逻辑和链表分裂类似，但最后多了一步判断：

```java
final void split(HashMap<K,V> map, Node<K,V>[] tab, int index, int oldCap) {
    TreeNode<K,V> b = this;
    TreeNode<K,V> loHead = null, loTail = null;
    TreeNode<K,V> hiHead = null, hiTail = null;
    int lc = 0, hc = 0;

    // 按链表方式遍历红黑树节点（利用 prev/next 指针）
    for (TreeNode<K,V> e = b; e != null; e = e.next) {
        if ((e.hash & oldCap) == 0) {
            // 同 lo 链表
            if ((e.prev = loTail) == null)
                loHead = e;
            else
                loTail.next = e;
            loTail = e;
            ++lc;
        } else {
            // 同 hi 链表
            if ((e.prev = hiTail) == null)
                hiHead = e;
            else
                hiTail.next = e;
            hiTail = e;
            ++hc;
        }
    }

    // 节点数 <= 6，退化为链表
    if (loHead != null) {
        if (lc <= UNTREEIFY_THRESHOLD)
            tab[index] = loHead.untreeify(map);
        else {
            tab[index] = loHead;
            if (hiHead != null)
                loHead.treeify(tab);
        }
    }

    if (hiHead != null) {
        if (hc <= UNTREEIFY_THRESHOLD)
            tab[index + oldCap] = hiHead.untreeify(map);
        else {
            tab[index + oldCap] = hiHead;
            if (loHead != null)
                hiHead.treeify(tab);
        }
    }
}
```

如果分裂后某条链表的节点数 `<= 6`，**红黑树退化为链表**（调用 `untreeify()`）。

---

## 面试追问

### Q1：HashMap 容量为什么必须是 2 的幂次？

两个原因：

1. **定位下标更快**：`table[i = (n - 1) & hash]` 比 `table[i = hash % n]` 更快
2. **扩容优化**：`hash & oldCap` 判断新位置的前提是容量是 2 的幂次

2 的幂次的二进制都是 1000...，减 1 后变成 111...，位与运算可以直接用 hash 的低位定位。

### Q2：负载因子为什么默认是 0.75？

这是**空间和时间的平衡**：

- 负载因子太小（0.5）：空间浪费，扩容频繁，但哈希碰撞少
- 负载因子太大（0.9）：空间利用率高，但哈希碰撞多，链表/红黑树变长

0.75 是一个经验值：当容量为 16 时，threshold 是 12，意味着平均每个桶 0.75 个元素，哈希分布相对均匀。

### Q3：扩容操作是线程安全的吗？

**不是**。扩容过程中，如果其他线程同时 put，可能导致数据丢失或不一致。

例如：线程 A 在扩容遍历，线程 B 向某个桶 put 新值，这个值可能在迁移过程中被遗漏。

### Q4：JDK 7 和 JDK 8 扩容有什么区别？

JDK 7 的扩容是在头部插入新节点（头插法），在并发扩容时可能产生**环形链表**，导致死循环。JDK 8 改用尾插法，避免了这个问题。

### Q5：threshold 达到 MAXIMUM_CAPACITY 后还能扩容吗？

不能。当 `oldCap >= MAXIMUM_CAPACITY` 时，JDK 直接把 threshold 设为 `Integer.MAX_VALUE`，并返回旧 table，不再扩容。这是为了防止整数溢出。

---

## 面试场景模拟：扩容相关的追问

:::details 点击展开：面试官的真实追问路径

**面试官**：HashMap 的扩容过程是怎样的？

**候选人甲**：就是元素超过阈值时扩容，创建新数组，把旧数据复制过去。

**面试官**：那 JDK 8 的扩容有什么优化？

**候选人甲**：...用了新的数组？

**面试官**（摇头）：JDK 8 不需要重新计算每个元素的 hash，只需要判断 hash & oldCap，你知道为什么吗？

---

**候选人乙**：JDK 8 扩容时，利用了容量是 2 的幂次这一特性。因为 oldCap 的二进制只有一位是 1（比如 16 = 10000），`hash & oldCap` 只看这一位就能判断新位置是在原位置还是原位置 + oldCap，不需要重新用 `(n-1) & hash` 计算。

**面试官**（点头）：那你说说 JDK 7 和 JDK 8 扩容有什么区别？

**候选人乙**：JDK 7 用头插法，在并发场景下可能产生环形链表，JDK 8 改成了尾插法，避免了这个问题。不过 HashMap 本身还是非线程安全的。

**面试官**（满意）：下一题。

:::

---

## 总结

HashMap 扩容的核心要点：

```
触发条件：++size > threshold（threshold = capacity * loadFactor）
扩容过程：
  1. 容量翻倍（oldCap * 2）
  2. 阈值翻倍（oldThr * 2）
  3. 遍历旧表迁移数据
     - 单节点：直接移动
     - 链表：hash & oldCap == 0 → 原位置；否则 → 原位置 + oldCap
     - 红黑树：分裂后节点 <= 6 则退化为链表

优化：预估容量，避免频繁扩容（O(n) 成本）
```

---

## 留给你的思考题

- [ ] JDK 8 中，`resize()` 方法在并发场景下有什么问题？考虑：两个线程同时检测到需要扩容（`size > threshold`），同时进入 `resize()`，执行了两次 `newTab = new Node[newCap]`，后面的线程可能看到错误的 table 状态。

- [ ] JDK 8 对并发扩容有什么处理？（提示：JDK 8 本身不处理并发问题；真正解决并发扩容的是 `ConcurrentHashMap` 的 `CAS + synchronized` 机制）

- [ ] 更根本的问题是：**HashMap 仍然不是线程安全的**——即使没有并发扩容，JDK 7 的头插法扩容还可能产生环形链表，你能说说这个环形链表是怎么形成的吗？
