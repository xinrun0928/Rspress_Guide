# Rspress 代码块规则

Rspress 使用 [Shiki](https://shiki.style) 在编译期完成语法高亮，高亮计算不增加运行时包体。支持的语言列表参考 [Shiki 支持的语言](https://shiki.style/languages)。

---

## 基本语法

**必须指定语言标识符**，不得使用纯 ```` ``` ````：

````mdx
```java
public class Hello {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
```
````

---

## meta 属性

### title（标题）

较长的代码块建议添加标题，置于语言标识符后：

````mdx
```java title="src/main/java/Hello.java"
public class Hello {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
```
````

### lineNumbers（显示行号）

由于 `rspress.config.ts` 中已配置 `showLineNumbers: true` 全局启用，**默认情况下所有代码块都会自动显示行号**，无需在每个代码块上单独指定 `lineNumbers`。

如需为特定代码块隐藏行号，使用 `lineNumbers=false` 覆盖：

````mdx
```java lineNumbers=false
// 此代码块不显示行号
public class Hello {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
```
````

### wrapCode（代码换行）

长行代码使用 `wrapCode` 自动换行：

````mdx
```java wrapCode
// 当全局启用 defaultWrapCode 时，可为特定代码块禁用换行
String sql = "SELECT id, name, email FROM users WHERE status = 'active' AND created_at > '2024-01-01' ORDER BY created_at DESC";
```
````

### fold（代码折叠）

长代码块使用 `fold` 启用展开/收起，`height` 自定义收起高度（默认 300px）：

````mdx
```java fold height="200"
// 此代码块收起高度为 200px
```
````

### 行高亮

配合 Shiki transformers（需在 `rspress.config.ts` 中配置），使用 `// [!code highlight]` 注释标记高亮行：

````mdx
```ts
function sum(a: number, b: number): number {
    return a + b; // [!code highlight]
}
// [!code highlight:3]
function multiply(a: number, b: number): number {
    return a * b;
}
```
````

### diff（差异对比）

使用 `diff` 语法标记增加和删除的行：

````mdx
```diff
function calculate(a, b) {
-   return a - b;
+   return a + b;
    return a * b;
}
```
````

### 错误与警告级别

使用 `// [!code error]` 和 `// [!code warning]` 标记错误和警告行：

````mdx
```ts
console.log('Normal'); // [!code warning]
console.error('Error'); // [!code error]
console.warn('Warning'); // [!code warning]
```
````

### focus（聚焦）

使用 `// [!code focus]` 突出当前行，淡化其他行：

````mdx
```ts
const a = 1; // [!code focus]
const b = 2;
const c = 3;
```
````

### 组合使用

多个 meta 属性可以组合，`lineNumbers` 在全局启用时可不写：

````mdx
```java wrapCode title="UserService.java"
// lineNumbers 已由全局配置默认启用，无需重复指定
public class UserService {
    public User findById(Long id) {
        return userRepository.findById(id).orElseThrow(() -> new UserNotFoundException(id));
    }
}
```
````

---

## 外部文件引用（file）

使用 `file="..."` 引用外部文件内容，避免文档中重复粘贴代码：

````mdx
```java file="./src/main/java/UserService.java"

```
````

路径支持：

| 路径类型 | 说明 | 示例 |
| --- | --- | --- |
| 相对路径 | 相对于当前 MDX 文件 | `./src/main/java/...` |
| 绝对路径 | 相对于项目根目录 | `<root>/src/main/java/...` |

> 引用外部文件时，文件名建议以 `_` 开头，配合[路由约定](/zh/guide/use-mdx/components.md#路由约定)。

---

> Badge 组件详细规范见 [.cursor/rules/rspress-badge.mdc](.cursor/rules/rspress-badge.mdc)

---

## 禁止事项

| 禁止行为 | 正确做法 |
| --- | --- |
| 省略语言标识符（纯 ```` ``` ````） | 必须指定语言，如 ```` ```java ``` ```` |
| 在代码块中使用 emoji 作为图标或注释 | 代码注释应简洁说明意图，而非逐行翻译；容器提示使用内置组件 |
| 手动在代码块内写行号（如 `1\| public class`） | 使用全局 `showLineNumbers` 配置，或在 meta 中使用 `lineNumbers`/`lineNumbers=false` |

---

## 表格与文本中的运算符格式

在 Markdown 表格的正文单元格中，`>=`、`<=`、`<`、`>` 等运算符应使用反引号包裹，以避免被 HTML 解析器识别为标签或造成渲染异常；在代码块中则无需额外处理。

````mdx
| 正确 | 错误 |
| --- | --- |
| `>=` | >= |
| `<=` | <= |
| `>` | > |
| `<` | < |
````

````mdx
```java
// 代码块中无需加反引号，直接写即可
if (nums[mid] >= target) { ... }
```
````
