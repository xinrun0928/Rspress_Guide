# Rspress `_meta.json` 与 `_nav.json` 配置规则

Rspress 使用 `_meta.json` 控制侧边栏，使用 `_nav.json` 控制导航栏。

---

## 目录结构

Rspress 文档目录结构遵循官方约定的默认布局，根目录为 `docs`：

```
docs/
├── _coverpage.mdx          # 覆盖默认首页（可选）
├── _footer.mdx             # 底部自定义内容（可选）
├── index.mdx               # 首页
├── _meta.json              # 根级侧边栏配置
│
├── guide/                  # 指南文档
│   ├── _meta.json          # 侧边栏分组与顺序配置
│   ├── index.mdx           # 指南入口
│   └── ...
│
├── advanced/               # 进阶文档（可选）
│   ├── _meta.json
│   ├── index.mdx
│   └── ...
│
└── api/                    # API 参考文档
    ├── _meta.json
    ├── index.mdx           # API 入口
    └── ...
```

`_meta.json` 用于控制侧边栏顺序和标题显示，**禁止手动硬编码侧边栏**。

---

## `_meta.json` 侧边栏配置

在 `docs/<子目录>/_meta.json` 中填入数组，每一项的类型如下：

### file（文件条目）

```json title="docs/guide/_meta.json"
[
  "introduction",
  {
    "type": "file",
    "name": "getting-started",
    "label": "快速上手"
  }
]
```

| 字段 | 说明 |
|------|------|
| `name` | 文件名，可带或不带 `.mdx` 后缀 |
| `label` | 侧边栏显示名称，不填则自动取文档 h1 标题 |
| `tag` | 标题后显示的图标，支持 SVG 字符串或图片 URL |
| `overviewHeaders` | Overview 页展示的标题级别，默认 `[2]`（h2） |

### dir（目录）

```json
{
  "type": "dir",
  "name": "advanced",
  "label": "进阶内容",
  "collapsible": true,
  "collapsed": false
}
```

| 字段 | 说明 |
|------|------|
| `collapsible` | 是否可折叠 |
| `collapsed` | 默认是否折叠 |

> 目录内如需点击后展示文档，建议创建 `index.mdx`，并在父级 `_meta.json` 中使用 `dir` 引用该目录。

### dir-section-header

与 `dir` 功能相同，仅 UI 上以分组标题形式展示，常用于第一层级：

```json
{
  "type": "dir-section-header",
  "name": "java",
  "label": "Java"
}
```

### divider（分割线）

```json
{ "type": "divider" }
```

`dashed: true` 可设为虚线。

### section-header（分组标题）

```json
{ "type": "section-header", "label": "第一组" }
```

常配合 `divider` 区分不同分组。

### custom-link（自定义链接）

```json
{
  "type": "custom-link",
  "label": "GitHub",
  "link": "https://github.com"
}
```

支持嵌套子链接：

```json
{
  "type": "custom-link",
  "label": "外部资源",
  "items": [
    { "type": "custom-link", "label": "GitHub", "link": "https://github.com" }
  ]
}
```

### 完整示例

```json title="docs/_meta.json"
[
  "index",
  {
    "type": "dir",
    "name": "guide",
    "label": "指南",
    "collapsible": true,
    "collapsed": false
  },
  {
    "type": "dir-section-header",
    "name": "java",
    "label": "Java"
  },
  "java/collection",
  "java/concurrent",
  {
    "type": "section-header",
    "label": "数据库"
  },
  {
    "type": "custom-link",
    "label": "MySQL 索引",
    "link": "/database/mysql/index"
  }
]
```

### 无配置用法

目录下仅有文档（无子目录）且对顺序无要求时，可不配置 `_meta.json`，Rspress 会按文件名字母顺序自动生成侧边栏。数字前缀可控制顺序：

```
basic/
├── 1-introduction.mdx
├── 2-install.mdx
└── 3-advanced.mdx
```

---

## `_nav.json` 导航栏配置

在 `docs/_nav.json` 中填入数组，类型与 `themeConfig.nav` 一致：

```json title="docs/_nav.json"
[
  { "text": "指南", "link": "/guide/", "activeMatch": "^/guide/" },
  { "text": "API", "link": "/api/", "activeMatch": "^/api/" }
]
```

> 导航栏和侧边栏仅在 `rspress.config.ts` 中未配置 `nav`/`sidebar` 时自动生效。
