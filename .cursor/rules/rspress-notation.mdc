# 符号转义规则

Rspress 的 MDX 解析器对部分符号处理存在限制，文本中直接使用时会导致渲染异常。所有比较运算符、赋值运算符在正文中出现时，必须用反引号包裹。

## 比较运算符

| 符号 | 正确写法 | 错误写法 |
|---|---|---|
| `<=` | `` `<=` `` | `<=` |
| `>=` | `` `>=` `` | `>=` |
| `==` | `` `==` `` | `==` |
| `!=` | `` `!=` `` | `!=` |
| `<` | `` `<` `` | `<` |
| `>` | `` `>` `` | `>` |

### 泛型类型语法（尖括号）

在 MDX 正文中出现泛型类型时（如 `HashSet<String>`、`HashMap<K, V>`），尖括号会被 MDX 解析器误认为 JSX 标签，导致渲染错误。需用反引号包裹：

| 场景 | 正确写法 | 错误写法 |
|---|---|---|
| 正文中的泛型类型 | `` `HashSet<String>` `` | `HashSet<String>` |
| 正文中的比较描述 | `` `<` `` | `<` |

#### 嵌套泛型：每个泛型类型单独反引号包裹

当泛型嵌套出现时（如 `WeakReference<ThreadLocal<?>>`、`Map<String, List<Object>>`），**不得将整个嵌套表达式放在一个反引号内**，否则尖括号仍会被 MDX 解析。应将每个独立的泛型类型分别用反引号包裹：

| 场景 | 正确写法 | 错误写法 |
|---|---|---|
| 嵌套泛型（完整表达） | `` `WeakReference` `ThreadLocal<?>` `` | `` `WeakReference<ThreadLocal<?>>` `` |
| 嵌套泛型（完整表达） | `` `Map` `String` `List` `Object` `` | `` `Map<String, List<Object>>` `` |

```mdx
# 正确
Entry 继承自 `WeakReference` `ThreadLocal<?>`。

# 错误（整个表达式用一对反引号，尖括号仍会触发 JSX 解析）
Entry 继承自 `WeakReference<ThreadLocal<?>>`。
```

> [!TIP]
> **判断方法**：将泛型表达式代入 JSX 语法检查。如果 `<` 后紧跟大写字母，MDX 解析器会将其视为标签开始，因此需要将每个泛型类型名（大写字母开头）单独包裹。

---

## 赋值与箭头运算符

| 符号 | 正确写法 | 错误写法 |
|---|---|---|
| `=` | `` `=` `` | `=` |
| `->` | `` `->` `` | `->` |
| `=>` | `` `=>` `` | `=>` |

---

## JSX 模板字面量（`${...}`）

在 MDX 正文中使用 `${variable-name}` 这类模板字符串语法时，`${` 会被 MDX 解析器识别为 JSX 模板表达式的开始，导致 ReferenceError。需将 `$` 转义为 `\${`：

|| 场景 | 正确写法 | 错误写法 |
||---|---|---|
|| 正文中的模板字符串 | `\${business}-async-pool` | `${business}-async-pool` |
|| 正文中的占位符 | 线程池名 `my\${poolName}` | 线程池名 `my${poolName}` |

```mdx
# 错误（MDX 将 ${business} 解析为 JSX 表达式）
业务线程池命名建议：`${business}-async-pool`

# 正确（转义 $ 符号）
业务线程池命名建议：`\${business}-async-pool`
```

> **判断**：看到 `$` 后紧跟 `{`，说明是模板字符串语法，需转义。在 JavaScript/Shell 代码块内无需转义。

---

## 使用场景

**问题**：旅行商问题的简化版，城市数量 `` `<=` `` 20，适合用状态压缩 DP。

**数据范围**：n `` `<=` `` 20，m `` `<=` `` 200。

**复杂度分析**：时间 `` `=` `` O(n²)，空间 `` `=` `` O(n)。

````mdx
```java
// 代码块中无需加反引号，直接写即可
if (nums[mid] >= target) { ... }
```
````

## 数组与对象语法（冒号、方括号、花括号）

在 MDX 正文（尤其引用块 `>` 或列表）中出现数组、对象字面量时，其中的冒号 `:`、方括号 `[]`、花括号 `{}` 可能被 MDX 解析器误处理。需用反引号包裹整个数组或对象：

| 场景 | 正确写法 | 错误写法 |
|---|---|---|
| 正文中的对象数组 sort | `` `sort: [{ sales: desc }, { _id: desc }]` `` | `sort: [{ sales: desc }, { _id: desc }]` |
| 正文中的 search_after | `` `search_after: [999, "abc"]` `` | `search_after: [999, "abc"]` |

> [!TIP]
> 若数组/对象已完整出现在代码块（```` ``` ````）内，**无需转义**，直接写即可。

## 分隔线（`---`）使用规范

不得在文档开头的描述段落之后、第一个 `##` 章节之前插入 `---` 作为装饰性分隔线。描述内容应直接过渡到正文结构，无需用分隔线制造视觉跳跃。

| 场景 | 正确做法 | 错误做法 |
|---|---|---|
| 开场描述后直接接章节 | 描述结尾直接写第一个 `##` | 描述后加 `---` 再写 `##` |
| 章节之间 | 适当留白或直接写下一章 `##` | 每个 `##` 前加 `---` |
| 逻辑分组处 | 用 `##` 章节区分，或用空行自然分隔 | 用 `---` 替代结构划分 |

```mdx
# 正确：描述直接过渡到正文

正文描述内容，一段或若干段。

## 一、第一个章节

内容。

---

# 错误：在描述后加装饰性分隔线

正文描述内容。

---

## 一、第一个章节

内容。
```

> 唯一的例外：需要明确区分「正文内容」与「延伸内容」时（如正文末尾与延伸阅读之间），可使用 `---` 分隔。

---

## 内部链接路径规范

引用同站点的其他文档时，链接路径**不得包含 `/docs` 前缀**。Rspress 的文档根目录为 `docs/`，站内链接应从 `docs/` 目录之后开始写。

| 场景 | 正确写法 | 错误写法 |
|---|---|---|
| 站内文档链接 | `/high-throughput/.../explain` | `/docs/high-throughput/.../explain` |
| 站内文档链接 | `./explain` | `./explain.mdx` |

> **判断**：站内链接去掉 `/docs` 后缀后，等同于相对于 `docs/` 的文件路径。不得在链接中出现 `.mdx` 等文件后缀，也不得使用 `../` 返回上级目录后再进入（应直接写绝对路径）。

---

## 注意事项

- 仅在**正文段落、列表、表格单元格、强调文本（`**粗体**`、`*斜体*`）**中出现时需要转义
- 在代码块（```` ``` ```` 或 ```` ```tsx ```` 等）内部使用时，**直接写符号即可**，无需转义

## 常见遗漏场景

以下场景极易忘记转义，必须格外注意：

### 行内强调文本（`**粗体**`）

粗体包裹的内容同样会被 MDX 解析，正文中的运算符同样需要转义：

| 场景 | 正确写法 | 错误写法 |
|---|---|---|
| 粗体内出现比较符 | `**核心原则：消费者数量 <= 分区数量**` | `**核心原则：消费者数量 <= 分区数量**` |
| 粗体内出现赋值符 | `**约定：max = 100**` | `**约定：max = 100**` |
| 粗体内出现箭头符 | `**签名：key -> value**` | `**签名：key -> value**` |

```mdx
# 错误（运算符未转义，粗体无法保护符号）
**核心原则**：消费者数量 <= 分区数量

# 正确
**核心原则**：消费者数量 `<=` 分区数量
```

### 数学表达式中的比较符

在正文叙述中描述数量关系时，比较符必须转义：

```mdx
# 错误
当 n <= 100 时，适用暴力解法。

# 正确
当 n `<=` 100 时，适用暴力解法。
```

### 表格单元格

表格单元格属于正文范畴，运算符必须转义：

```mdx
# 错误
| 条件 | 结果 |
| --- | --- |
| n <= 100 | 暴力解 |

# 正确
| 条件 | 结果 |
| --- | --- |
| n `<=` 100 | 暴力解 |
```
