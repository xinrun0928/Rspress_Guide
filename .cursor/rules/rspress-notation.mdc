---
description: 常见错误
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

# 符号转义规则

正文段落、列表、表格单元格中出现以下符号时，必须用反引号包裹；代码块内无需转义。

## 比较运算符与赋值符

| 符号 | `=` | `<=` | `>=` | `==` | `!=` | `->` | `=>` |
| --- | --- | --- | --- | --- | --- | --- | --- |

## 泛型类型（尖括号）

MDX 解析器将尖括号视为 JSX 标签，泛型类型需用反引号包裹：

| 场景 | 正确写法 | 错误写法 |
| --- | --- | --- |
| 单层泛型 | `HashSet<String>` | `HashSet<String>` |
| 嵌套泛型 | `WeakReference` `ThreadLocal<?>` | `WeakReference<ThreadLocal<?>>` |

判断方法：`<` 后紧跟大写字母，会被解析为标签，需拆分每个泛型类型单独反引号。

## JSX 模板字面量（`${...}`）

`${` 会被解析为 JSX 表达式，需转义为 `\${`：

```mdx
# 错误
业务线程池命名建议：`${business}-async-pool`

# 正确
业务线程池命名建议：`\${business}-async-pool`
```

## 粗体内运算符

`**粗体**` 包裹的内容同样会被 MDX 解析，运算符必须转义：

```mdx
# 错误
**核心原则**：消费者数量 <= 分区数量

# 正确
**核心原则**：消费者数量 `<=` 分区数量
```

## 表格单元格

表格单元格属于正文范畴，运算符必须转义：

```mdx
| 条件 | 结果 |
| --- | --- |
| n `<=` 100 | 暴力解 |
```

## 数组与对象字面量

正文或引用块中出现数组、对象字面量时，需用反引号包裹整个表达式：

| 场景 | 正确写法 |
| --- | --- |
| 对象数组 sort | `sort: [{ sales: desc }, { _id: desc }]` |
| search_after | `search_after: [999, "abc"]` |

代码块内无需转义。

## 分隔线（`---`）使用规范

禁止在段落内容之后、下一级标题之前插入 `---` 作为装饰性分隔线。内容应直接过渡到下一节标题。

错误示例：

```md
# 一级标题

内容段落。

---

## 二级标题
```

正确示例：

```md
# 一级标题

内容段落。

## 二级标题
```

唯一例外：需要明确区分「正文内容」与「延伸内容」时，可使用 `---` 分隔。

## 内部链接路径规范

站内链接不得包含 `/docs` 前缀，不得出现 `.mdx` 后缀：

| 场景 | 正确写法 | 错误写法 |
| --- | --- | --- |
| 站内文档链接 | `/high-throughput/.../explain` | `/docs/high-throughput/.../explain` |
| 站内文档链接 | `./explain` | `./explain.mdx` |
