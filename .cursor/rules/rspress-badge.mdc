# Rspress 内联徽章规则

Badge 组件用于在文本中标注小型信息，适合标记版本号、技术栈、状态等。

---

## 基本用法

````mdx
```mdx
import { Badge } from '@rspress/core/theme';

<Badge text="NEW" type="info" />
```
````

## 可用类型

````mdx
```mdx
<Badge text="tip" type="tip" />
<Badge text="info" type="info" />
<Badge text="warning" type="warning" />
<Badge text="danger" type="danger" />
```
````

## 轮廓样式

````mdx
```mdx
<Badge text="tip" type="tip" outline />
<Badge text="info" type="info" outline />
<Badge text="warning" type="warning" outline />
<Badge text="danger" type="danger" outline />
```
````

## 与文本内联使用

````mdx
```mdx
// Badge 与文本自然衔接，中文场景注意间距
Dubbo 支持多种协议 <Badge text="Dubbo2" type="info" />，
其中 Triple 协议基于 gRPC <Badge text="gRPC" type="tip" outline />。
```
````

## 在标题中使用

````mdx
```mdx
### H3 标题 <Badge text="Danger" type="danger" />

#### H4 标题 <Badge text="Warning" type="warning" />

##### H5 标题 <Badge text="Info" type="info" />
```
````

## Props 参考

| Prop | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `text` | `string` | — | 徽章显示的文本 |
| `type` | `'tip' \| 'info' \| 'warning' \| 'danger'` | `'tip'` | 徽章颜色类型 |
| `outline` | `boolean` | `false` | 是否使用轮廓样式 |
| `children` | `React.ReactNode` | — | 自定义内容，传入后 `text` 无效 |

---

## 使用原则

| 场景 | 推荐用法 |
|---|---|
| 标注技术栈、版本号、协议名 | `<Badge text="gRPC" type="tip" />` |
| 标注注意事项、风险提示 | `<Badge text="JDK 8+" type="warning" />` |
| 标注废弃、危险、破坏性内容 | `<Badge text="Deprecated" type="danger" />` |
| 在标题中作为副标签 | `### 标题 <Badge text="Info" type="info" />` |

> **避免滥用**：Badge 是轻量标注，不适合大段文字或复杂内容。同一行不宜出现超过两个 Badge。
