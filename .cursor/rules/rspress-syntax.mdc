---
description: Rspress 基础语法规则 — 代码块、容器、徽章
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

# Rspress 基础语法规则

Rspress 使用 [Shiki](https://shiki.style) 在编译期完成语法高亮。

## 代码块

### 基本语法

**必须指定语言标识符**，不得使用纯 ```` ``` ````：

````mdx
```java
public class Hello {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
```
````

### meta 属性

| 属性 | 说明 |
|---|---|
| `title="..."` | 代码块标题 |
| `lineNumbers` | 显示行号（默认已全局启用，无需重复指定） |
| `lineNumbers=false` | 禁用行号 |
| `wrapCode` | 长行自动换行 |
| `fold height="200"` | 折叠代码，收起高度 200px |

````mdx
```java title="src/main/java/Hello.java" wrapCode
// 行号已由全局配置默认启用
public class Hello {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
```
````

### 行注释语法（Shiki transformers）

| 注释 | 效果 |
|---|---|
| `// [!code highlight]` | 高亮当前行 |
| `// [!code highlight:N]` | 高亮连续 N 行 |
| `// [!code error]` | 错误级别 |
| `// [!code warning]` | 警告级别 |
| `// [!code focus]` | 聚焦当前行，淡化其他行 |

````mdx
```diff
function calculate(a, b) {
-   return a - b;
+   return a + b;
    return a * b;
}
```
````

### 外部文件引用

````mdx
```java file="./src/main/java/UserService.java"
```
````

路径支持相对路径（相对于当前 MDX 文件）和绝对路径（相对于项目根目录）。

### 禁止事项

| 禁止行为 | 正确做法 |
|---|---|
| 省略语言标识符（纯 ```` ``` ````） | 必须指定语言，如 ```` ```java ``` ```` |
| 在代码块中使用 emoji 作为图标或注释 | 使用内置容器组件 |
| 手动写行号（如 `1\| public class`） | 使用 `lineNumbers` meta 或全局配置 |

---

## 容器

### `:::` 语法

````mdx
:::tip 自定义标题

内容

:::

:::warning
这是 warning 类型
:::

:::danger
危险操作
:::

:::details 点击展开

可折叠内容

:::
````

**类型**：`note`、`tip`、`info`、`warning`、`danger`、`details`。

**自定义标题**：推荐直接在类型后加空格和标题，如 `:::tip 自定义标题`。MDX 中大括号语法需转义（`\{...}`），应避免。

### GitHub Alerts 语法

````mdx
> [!TIP]
> 提示内容

> [!WARNING]
> 警告内容

> [!DANGER]
> 危险内容
````

### 间距规范

容器标记 `:::` 前后必须保留空行，内容与 `:::` 闭合之间各空一行：

````mdx
这是正文。

:::tip 提示

内容，与前后各空一行。

:::

正文继续。
````

### 类型与语义对照

| 类型 | 语义 |
|---|---|
| `note` | 补充说明、背景信息 |
| `tip` | 实用技巧、建议 |
| `info` | 额外信息、参考资料 |
| `warning` | 需要注意的潜在问题 |
| `danger` | 破坏性操作、不可逆后果 |
| `details` | 可展开的详细信息 |

### 禁止事项

| 禁止行为 | 正确做法 |
|---|---|
| 大写容器类型（如 `:::Tip`） | 必须小写（如 `:::tip`） |
| 用 emoji 代替容器 | 使用内置容器组件 |

---

## 徽章（Badge）

Badge 用于在文本中标注小型信息，适合版本号、技术栈、状态等。

### 基本用法

````mdx
import { Badge } from '@rspress/core/theme';

<Badge text="JDK 8+" type="tip" />
<Badge text="Deprecated" type="danger" />
<Badge text="gRPC" type="tip" outline />
````

### 可用类型

`type` 支持 `tip`、`info`、`warning`、`danger`，默认 `tip`。`outline` 属性切换轮廓样式。

### 使用原则

| 场景 | 推荐用法 |
|---|---|
| 标注版本号、技术栈、协议名 | `<Badge text="gRPC" type="tip" />` |
| 标注注意事项、风险提示 | `<Badge text="JDK 8+" type="warning" />` |
| 标注废弃、破坏性内容 | `<Badge text="Deprecated" type="danger" />` |

> **避免滥用**：同一行不宜超过两个 Badge，不适合大段文字或复杂内容。

---

## 目录与导航

### `_meta.json` 侧边栏配置

`_meta.json` 控制侧边栏顺序和标题显示，**禁止手动硬编码侧边栏**。

**类型**：`file`、`dir`、`dir-section-header`、`divider`、`section-header`、`custom-link`。

````mdx title="docs/_meta.json"
[
  "index",
  {
    "type": "dir",
    "name": "guide",
    "label": "指南",
    "collapsible": true,
    "collapsed": false
  },
  {
    "type": "dir-section-header",
    "name": "java",
    "label": "Java"
  },
  "java/collection",
  {
    "type": "section-header",
    "label": "数据库"
  },
  {
    "type": "custom-link",
    "label": "MySQL 索引",
    "link": "/database/mysql/index"
  }
]
````

**常用条目类型**：

| 类型 | 说明 |
|---|---|
| `file` | 单个文件，`name` 为文件名，`label` 为侧边栏显示名（可选） |
| `dir` | 目录，可折叠，`collapsible`/`collapsed` 控制折叠行为 |
| `dir-section-header` | 目录类型，UI 上以分组标题展示，常用于第一层级 |
| `section-header` | 分组标题，配合 `divider` 区分不同分组 |
| `custom-link` | 自定义链接，支持嵌套 `items` |
| `divider` | 分割线，`dashed: true` 可设为虚线 |

````mdx
{ "type": "file", "name": "getting-started", "label": "快速上手" }

{ "type": "divider" }

{ "type": "section-header", "label": "第一组" }

{
  "type": "custom-link",
  "label": "外部资源",
  "items": [
    { "type": "custom-link", "label": "GitHub", "link": "https://github.com" }
  ]
}
````

> 目录下仅有文档且对顺序无要求时，可不配置 `_meta.json`，Rspress 按文件名字母顺序自动生成侧边栏。数字前缀可控制顺序，如 `1-introduction.mdx`。

### `_nav.json` 导航栏配置

````mdx title="docs/_nav.json"
[
  { "text": "指南", "link": "/guide/", "activeMatch": "^/guide/" },
  { "text": "API", "link": "/api/", "activeMatch": "^/api/" }
]
````

> 仅在 `rspress.config.ts` 中未配置 `nav`/`sidebar` 时自动生效。
