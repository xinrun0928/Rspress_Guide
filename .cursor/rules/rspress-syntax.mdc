---
description: Rspress 基础语法规则 — 代码块、容器、徽章、格式
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

# Rspress 基础语法规则

Rspress 使用 [Shiki](https://shiki.style) 在编译期完成语法高亮，所有代码块在构建阶段完成渲染，无需运行时开销。

## 一、代码块

### 必须指定语言标识符

所有代码块必须显式指定语言标识符，不得使用纯反引号包裹。

错误示例（无语言标识符）：

````
```java
public class Hello {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
```
````

正确示例：

```java
public class Hello {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
```

### meta 属性

| 属性 | 说明 |
| --- | --- |
| `title="..."` | 代码块标题，显示在左上角 |
| `lineNumbers=false` | 禁用行号（行号默认全局启用，一般不需要显式指定） |
| `wrapCode` | 长行自动换行，避免溢出 |
| `fold height="200"` | 折叠代码，收起时显示高度 200px |

多个属性可以叠加使用：

```java title="ThreadPoolConfig.java" wrapCode
ThreadPoolExecutor executor = new ThreadPoolExecutor(
    corePoolSize,
    maxPoolSize,
    keepAliveTime,
    TimeUnit.SECONDS,
    new LinkedBlockingQueue<>(queueCapacity),
    new ThreadPoolExecutor.CallerRunsPolicy()
);
```

### 行注释语法（Shiki transformers）

通过在代码行末添加特定注释，可实现视觉高亮效果：

| 注释 | 效果 |
| --- | --- |
| `// [!code highlight]` | 高亮当前行 |
| `// [!code highlight:N]` | 高亮连续 N 行 |
| `// [!code error]` | 标记为错误级别（红色） |
| `// [!code warning]` | 标记为警告级别（黄色） |
| `// [!code focus]` | 聚焦当前行，其他行淡化 |

```java
ThreadPoolExecutor executor = new ThreadPoolExecutor(
    8,                              // [!code highlight]
    16,                             // [!code highlight]
    60, TimeUnit.SECONDS,
    new LinkedBlockingQueue<>(1000), // [!code warning]
    new ThreadPoolExecutor.CallerRunsPolicy() // [!code focus]
);
```

diff 块使用 `diff` 语言标识符，`-` 表示删除行，`+` 表示新增行：

```diff
function calculate(a, b) {
-   return a - b;
+   return a + b;
    return a * b;
}
```

### 外部文件引用

使用 `file` 属性引用外部文件，路径支持相对于当前文档或项目根目录：

```java file="./src/main/java/UserService.java"
```

## 二、代码呈现规范

### 渐进式展示

不要只贴最终答案。从坏到好逐步演进，展示思考过程。

推荐顺序：错误示例 → 正确示例 → 生产级版本。

### 代码注释

告诉「为什么这样做」，而不是「代码做了什么」。

错误示例（只描述做什么）：

```java
// 设置线程名前缀
threadFactory.setNamePrefix("biz-pool-");
```

正确示例（解释原因和权衡）：

```java
// 带上业务标识，便于出问题后快速定位是哪个线程池在抢资源
// 不要只写 "pool-"，否则全链路排查时无法区分来源
threadFactory.setNamePrefix("biz-pool-");
```

### 禁止事项

- 省略代码块语言标识符
- 在代码注释中使用 emoji
- 手动写行号（如 `1| public class`）

## 三、容器

### `:::` 语法

只允许使用以下六种类型：`note`、`tip`、`info`、`warning`、`danger`、`details`。类型必须小写。

类型后直接加空格和自定义标题，也可不加标题：

```mdx
:::tip 自定义标题

内容

:::

:::warning
这是 warning 类型
:::

:::details 点击展开查看详情

可折叠内容

:::
```

### GitHub Alerts 语法

```mdx
> [!TIP]
> 提示内容

> [!WARNING]
> 警告内容
```

### 禁止事项

- 容器类型必须小写，禁止写 `:::Tip`、`:::Warning`
- 禁止用 emoji 代替容器

## 四、禁止使用 Badge 组件（强制）

docs 目录下所有 `.mdx` 文件一律不得使用 `<Badge>` 组件，一律使用纯文本替代：

| 场景 | 推荐写法 | 示例 |
| --- | --- | --- |
| 标签 + 说明文字 | `**标签**：说明` | `**推荐**：优先使用连接池` |
| 纯标签 | `\|标签\|` | `\|JDK 17+\|` |
| 列表中标注 | `\|JDK 8+\| \|JDK 17+\|` | - |

错误示例：

```mdx
<Badge type="tip" text="推荐" />
```

正确示例：

```md
**推荐**：优先使用连接池。
```

## 五、目录与导航

### `_meta.json` 侧边栏配置

`_meta.json` 控制侧边栏顺序和标题显示，禁止手动硬编码侧边栏。

类型：`file`（指向文件）、`dir`（指向目录）、`section-header`（纯标题）、`custom-link`（自定义链接）、`divider`（分隔线）。

```json title="docs/_meta.json"
[
  { "type": "file", "name": "introduction", "label": "简介" },
  { "type": "file", "name": "getting-started", "label": "快速开始" },
  { "type": "divider" },
  { "type": "dir", "name": "advanced", "label": "进阶内容" }
]
```

目录下仅有文档且对顺序无要求时，可不配置 `_meta.json`，Rspress 按文件名字母顺序自动生成侧边栏。数字前缀可控制顺序，如 `1-introduction.mdx`。

### `_nav.json` 导航栏配置

```json title="docs/_nav.json"
[
  { "text": "指南", "link": "/guide/", "activeMatch": "^/guide/" },
  { "text": "API", "link": "/api/", "activeMatch": "^/api/" }
]
```

> 仅在 `rspress.config.ts` 中未配置 `nav`/`sidebar` 时自动生效。

## 六、正文格式规范

**段落格式**：句间不空行，保持阅读节奏流畅。空行仅用于分隔不同段落、标题前后、代码块前后。不要一句话一段，不要一个段落过长。

**待办清单**：使用 GFM 语法 `- [ ]` / `- [x]`，不要使用 Unicode 符号 `☐` / `☑`。

```md
- [x] 已完成
- [ ] 待完成
```

**分隔线（`---`）使用规范**：禁止在段落内容之后、下一级标题之前插入 `---` 作为装饰性分隔线。内容应直接过渡到下一节标题。唯一例外是需要明确区分「正文内容」与「延伸内容」时。

错误示例：

```md
## 线程池参数

---

正文内容
```

正确示例：

```md
## 线程池参数

正文内容

## 下一节标题
```

**内部链接路径**：站内链接不得包含 `/docs` 前缀，不得出现 `.mdx` 后缀。

错误示例：

```md
[线程池详解](/docs/java/thread-pool.mdx)
[快速开始](./getting-started.mdx)
```

正确示例：

```md
[线程池详解](/java/thread-pool)
[快速开始](./getting-started)
```