---
description: Rspress 基础语法规则 — 代码块、容器、徽章、格式
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

# Rspress 基础语法规则

Rspress 使用 [Shiki](https://shiki.style) 在编译期完成语法高亮。

## 代码块

必须指定语言标识符，不得使用纯 ```` ``` ````：

```java
public class Hello {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
```

### meta 属性

| 属性 | 说明 |
| --- | --- |
| `title="..."` | 代码块标题 |
| `lineNumbers` | 显示行号（默认已全局启用，无需重复指定） |
| `lineNumbers=false` | 禁用行号 |
| `wrapCode` | 长行自动换行 |
| `fold height="200"` | 折叠代码，收起高度 200px |

### 行注释语法（Shiki transformers）

| 注释 | 效果 |
| --- | --- |
| `// [!code highlight]` | 高亮当前行 |
| `// [!code highlight:N]` | 高亮连续 N 行 |
| `// [!code error]` | 错误级别 |
| `// [!code warning]` | 警告级别 |
| `// [!code focus]` | 聚焦当前行，淡化其他行 |

```diff
function calculate(a, b) {
-   return a - b;
+   return a + b;
    return a * b;
}
```

### 外部文件引用

```java file="./src/main/java/UserService.java"
```

路径支持相对路径（相对于当前 MDX 文件）和绝对路径（相对于项目根目录）。

### 禁止事项

- 省略代码块语言标识符
- 在代码块中使用 emoji 作为图标或注释
- 手动写行号（如 `1\| public class`）

## 代码呈现

**渐进式展示**：从坏到好，逐步演进。不要只贴最终答案，要展示思考过程。

**代码注释**：告诉「为什么」而不是「是什么」。

## 容器

### `:::` 语法

类型：`note`、`tip`、`info`、`warning`、`danger`、`details`。自定义标题直接在类型后加空格和标题。

```mdx
:::tip 自定义标题

内容

:::

:::warning
这是 warning 类型
:::

:::details 点击展开

可折叠内容

:::
```

### GitHub Alerts 语法

```mdx
> [!TIP]
> 提示内容

> [!WARNING]
> 警告内容
```

### 禁止事项

- 大写容器类型（如 `:::Tip`）——必须小写
- 用 emoji 代替容器

## 禁止使用 Badge 组件（强制）

docs 目录下所有 `.mdx` 文件一律不得使用 `<Badge>` 组件，一律使用纯文本替代：

| 场景 | 推荐写法 |
| --- | --- |
| 标签 + 说明文字 | `**标签**：说明` |
| 纯标签 | `\|标签\|` |
| 列表中标注 | `\|JDK 8+\| \|JDK 17+\|` |

## 目录与导航

### `_meta.json` 侧边栏配置

`_meta.json` 控制侧边栏顺序和标题显示，禁止手动硬编码侧边栏。类型：`file`、`dir`、`dir-section-header`、`section-header`、`custom-link`、`divider`。

目录下仅有文档且对顺序无要求时，可不配置 `_meta.json`，Rspress 按文件名字母顺序自动生成侧边栏。数字前缀可控制顺序，如 `1-introduction.mdx`。

### `_nav.json` 导航栏配置

```json title="docs/_nav.json"
[
  { "text": "指南", "link": "/guide/", "activeMatch": "^/guide/" },
  { "text": "API", "link": "/api/", "activeMatch": "^/api/" }
]
```

> 仅在 `rspress.config.ts` 中未配置 `nav`/`sidebar` 时自动生效。

## 正文格式规范

**段落格式**：句间不空行，保持阅读节奏流畅。空行仅用于分隔不同段落、标题前后、代码块前后。

**待办清单**：使用 GFM 语法 `- [ ]` / `- [x]`，不要使用 Unicode 符号 `☐` / `☑`。

**分隔线（`---`）**：不得在 `#` 或 `##` 标题正下方、描述段落之后插入 `---` 作为装饰性分隔线。唯一例外是需要明确区分「正文内容」与「延伸内容」时。

**内部链接路径**：站内链接不得包含 `/docs` 前缀，不得出现 `.mdx` 后缀。
