---
description: ArchNexus 文档编写规范 — 写作风格
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

# 写作风格规则

> 好的架构文档，不是 API 参数表，不是概念堆砌，更不是「复制官网再换个说法」。它应该像一场高质量的技术评审：从真实问题出发，逐层拆解设计背景、核心原理、关键权衡与落地方案。读者读完之后，不只是知道「怎么做」，更知道「为什么这么做、什么时候不该这么做、出了问题该怎么排查」。真正优秀的文档，最终要帮助读者建立自己的设计思维。

## 一、开场设计

大多数读者会在前 30 秒决定这篇文章值不值得继续读。所以开场不要急着「介绍本文内容」，而是先把读者拉进问题里。

### 开场的核心目标

好的开场至少要做到下面三件事中的两件：

1. 让读者产生代入感
2. 让读者意识到问题的重要性
3. 让读者愿意继续往下读

### 常见开场模板

| 模板 | 适用场景 | 示例 |
|---|---|---|
| 场景切入 | 故障排查、线上问题、架构设计 | 凌晨 2 点，发布刚结束，数据库连接池突然被打满，接口开始大量超时。你第一反应是数据库慢了，但真正的问题，可能根本不在数据库。 |
| 痛点共鸣 | 常见误区、踩坑总结 | 明明已经加了缓存，系统还是慢；明明配置了线程池，CPU 却还是打满——问题到底出在哪？ |
| 核心追问 | 原理类、底层机制 | 一个 HTTP 请求发出去之后，JVM 到底做了什么？线程、Socket、缓冲区、序列化，谁才是真正的性能瓶颈？ |
| 反差对比 | 打破认知、性能测试 | 很多人觉得「多线程一定更快」。但在线程数超过某个阈值之后，吞吐量反而开始下降。为什么？ |
| 悬念制造 | 引入新概念、复杂设计 | 这个设计只改了一行配置，却让系统的稳定性提升了一个数量级。它到底改了什么？ |
| 一行代码开场 | 最佳实践、参数分析 | `config.setMaxConnections(100);` —— 这个数字看起来很普通，但它背后其实是一整套容量评估模型。 |
| 数字开场 | 性能分析、复杂度问题 | 90% 的线上雪崩事故，都不是因为服务挂了，而是因为「还没挂的时候没有限流」。 |
| 历史演进 | 版本差异、技术演化 | 在没有消息队列的时候，系统是怎么做异步削峰的？答案比你想象得更「土」，但也更实用。 |

> 不必每篇都套模板，选最适合主题的那个。一篇文档只用一个开场，不要叠加。

### 开场避坑

**不要这样写**：

- ❌ 随着互联网的发展……
- ❌ 本文将介绍……
- ❌ 本节主要讲解……
- ❌ 在日常开发中，我们经常会……
- ❌ 大家都知道……

这些开场没有信息量，也没有情绪张力。直接进入问题、冲突、现象、错误结论或反直觉事实。因为读者不是来「看目录」的，而是来「解决问题」的。

## 二、文章结构设计

技术文档最大的失败，不是写错，而是写乱。一篇文章如果没有结构，读者读到一半就会失去方向——不知道为什么突然开始讲这个，不知道这一段和上一段有什么关系，不知道重点到底是什么。

### 推荐结构：漏斗式展开

从「大问题」逐步聚焦到「小问题」。推荐顺序：

1. 问题背景
2. 为什么现有方案不够
3. 核心设计思路
4. 关键原理拆解
5. 实际使用方式
6. 常见问题与边界条件
7. 适用场景与不适用场景
8. 总结与延伸思考

例如讲 Redis 缓存穿透，不要一上来就讲布隆过滤器。应该先讲：为什么缓存会失效 → 穿透、击穿、雪崩分别是什么 → 为什么空值缓存不够 → 布隆过��器解决了什么问题 → 布隆过滤器本身又有什么代价。这样读者会觉得整个知识链路是完整的，而不是「突然冒出一个技术名词」。

### 推荐结构：阶梯式推进

让读者每一节都有「学会了一个东西」的感觉。推荐顺序：

1. 最简版本：先跑通
2. 完整版本：加异常处理、日志、边界逻辑
3. 生产版本：加监控、重试、限流、熔断
4. 原理版本：解释为什么这样设计
5. 调优版本：说明还能怎么优化

这样读者不会因为一开始信息量过大而放弃。

## 三、节奏控制

一篇文章如果从头到尾都是一种节奏，读者会疲劳。

### 慢节奏适合讲什么

适合：核心原理、复杂机制、设计权衡、故障排查过程、性能瓶颈分析。

慢节奏要做三件事：多追问、多拆解、多解释「为什么」。

例如，不要只写「线程池满了会触发拒绝策略」，而要继续追问：为什么会满？是核心线程数太小，还是队列太短？为什么有些场景应该扩大队列，而有些场景应该减少队列？为什么 CallerRunsPolicy 有时候反而更危险？

### 快节奏适合讲什么

适合：配置步骤、命令说明、参数列表、重复性代码、环境准备。这些部分不要啰嗦，用户要的是快速获取信息，而不是读散文。

### 节奏切换建议

一篇 3000 字左右的文章，至少应该有 2~3 次节奏变化。例如：

- 开场：快节奏，引发兴趣
- 原理：慢节奏，深入讲解
- 配置：快节奏，快速落地
- 故障案例：慢节奏，增强理解
- 总结：快节奏，收束全文

## 四、表达技巧

### 1. 多用对比，而不是单独介绍

不要只说一个方案好。一定要回答：它比什么好？好在哪里？代价是什么？什么场景下反而更差？

例如：「本地缓存延迟更低，但无法跨实例共享；Redis 可以共享，但增加了网络开销和一致性问题。」这种对比，比单独介绍两个技术更容易让读者形成判断。

### 2. 多用数字，而不是模糊表达

不要写「数据量大时会变慢」「请求很多时可能有问题」「配置不合理会影响性能」。

应该写「当单表超过 5000 万行后，索引维护成本会明显上升」「在 10 万 QPS 下，p99 延迟可能从 5ms 上升到 200ms」。数字会让结论更可信。

### 3. 多用转折，增强阅读张力

例如：

- 很多人以为……但真正的问题在于……
- 表面上看这是网络问题，但实际是线程池阻塞导致的连锁反应
- 大多数教程会告诉你这样配置就够了，但线上问题往往出现在边界条件

文章有转折，读者才会有「继续读下去」的动力。

### 4. 多用类比，但不要滥用

复杂概念可以借助类比帮助理解。例如：熔断器像保险丝、限流像高速收费站、消息队列像快递柜、布隆过滤器像门卫名单、分布式锁像公共厕所门口的「有人/无人」标识。

但类比只能帮助入门，后面必须回到真实机制。

### 5. 善用架构图描述

涉及系统设计时，先给出整体架构图，再逐步展开各组件的职责和交互。

## 五、代码示例规范

代码不是越多越好，而是越「能说明问题」越好。

### 好代码示例的标准

1. 可以直接运行
2. 尽量完整，不要只截半段
3. 变量名有意义
4. 代码后面有解释
5. 能体现真实业务场景

### 推荐代码组织方式

**第一段：最简示例**——让读者先跑起来。

**第二段：生产版本**——增加日志、异常处理、参数校验、超时控制、限流/重试。

**第三段：错误示例**——告诉读者「这样写为什么危险」。很多时候，错误示例比正确示例更有价值，因为线上事故往往都来自「看起来没问题」的代码。

## 六、架构类文章必须回答的核心问题

架构设计类文章不是介绍某个组件怎么用，而是解释为什么要这样设计。必须回答：

1. **它解决了什么问题？** 没有问题背景的设计，是空中楼阁。
2. **为什么不用更简单的方案？** 复杂方案一定有代价。如果单机就能解决，不要硬上分布式。
3. **它的代价是什么？** 任何架构设计都有 trade-off。例如：引入缓存提升性能，但增加一致性问题；引入 MQ 提升削峰能力，但增加消息丢失和重复消费风险。
4. **什么场景下不应该用？** 真正成熟的架构师不是知道「什么能用」，而是知道「什么时候别用」。这是文章质量最容易拉开差距的地方。

## 七、真实项目视角

ArchNexus 的定位不是「技术百科」，而是「架构师成长之路」。所以文档必须尽量贴近真实项目。

推荐加入的内容：线上踩坑案例、压测数据、故障复盘、架构演进过程、为什么最初方案不够、为什么后来改成现在这样、如果重新设计会怎么做。

推荐句式：

- 如果是小团队，我会优先选择……
- 如果系统 QPS 不高，其实没必要……
- 如果线上已经出现……问题，那么优先排查……
- 这个方案前期很舒服，但业务复杂之后会开始暴露……问题
- 我在项目里踩过一个坑：……

这些内容最能体现「经验价值」。

## 八、字数与深度要求

| 类型 | 字数建议 | 目标 |
|---|---|---|
| 工具型 | 不限制 | 快速解决问题，准确和清晰是核心 |
| 普通文章 | `>= 1500` | 建立完整知识体系 |
| 原理类 | `>= 3000` | 讲透底层机制与设计逻辑 |
| 架构设计类 | `>= 2000` | 讲清 trade-off、适用场景与边界 |

### 普通文章至少回答四个问题

1. 这个技术怎么来的？
2. 为什么要这样设计？
3. 和同类方案相比有什么优劣？
4. 实际怎么用、容易踩什么坑？

### 原理类文章再多回答两个问题

5. 它的代价是什么？
6. 项目里通常怎么落地？

### 架构设计类文章重点回答三个问题

1. 解决了什么问题？
2. 不用它的代价是什么？
3. 什么场景下不应该用？

> 不是字数竞赛，是防止文档停在「入门级」却自以为写透了。

## 九、结尾设计

不要用「本文到这里就结束了」这种收尾。好的结尾应该让读者继续思考。推荐三种方式：

**方式一：抛出新问题**。例如：「既然线程池参数会影响系统稳定性，那么这些参数到底应该如何根据机器配置和业务类型来推导？」

**方式二：延伸对比**。例如：「Redis 能解决缓存问题，但如果缓存和数据库之间的数据一致性要求极高，仅靠 Redis 还够吗？」

**方式三：回到开场**。开头提到线上故障，结尾回到那个问题，形成闭环。这会让整篇文章更完整。

## 十、最终判断标准

写完一篇文章之后，不要先问「我写得全不全」，而要问：

1. 读者读完，能不能真正理解为什么这样设计？
2. 读者下次遇到类似问题，能不能自己做判断？
3. 读者看完之后，能不能直接落地？
4. 读者是否知道什么时候不该这样做？

如果答案是「能」，这篇文章才算真的写好了。