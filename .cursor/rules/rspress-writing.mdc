---
description: Rspress 知识库文档编写规范 — 写作风格、面试专题
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

# 写作风格规则

> 好的技术文档不是教科书，不是 API 手册，更不是面试题库的堆砌。它是一场对话——在关键处停顿、抛出问题、给出悬念，直到读者发出「原来如此」的感叹。

## 开场设计

### 七种开场模板

| 模板 | 适用场景 | 示例 |
|---|---|---|
| 场景切入 | 痛点问题、bug 排查 | 凌晨 2 点，你正准备上线新功能，数据库连接池突然耗尽……这是怎么回事？ |
| 痛点共鸣 | 常见误区、认知陷阱 | synchronized 用了，代码还是有问题——问题到底在哪？ |
| 核心追问 | 原理讲解、高频面试题 | HashMap.get() 的时候，JVM 到底做了什么？追问下去，十个人里有九个答不上来。 |
| 反差对比 | 打破固有认知 | 很多人说 synchronized 性能很差。但 JDK 6 之后，JVM 团队花了整整三年优化它。 |
| 悬念制造 | 引入新概念 | HashMap 在 JDK 8 之后，永远不会出现死循环了。这是真的吗？ |
| 一行代码开场 | 技术对比、最佳实践 | `List<String> list = new ArrayList<>();` — 这行代码里，藏着一个性能优化的秘密。 |
| 数字开场 | 性能分析、复杂度讲解 | 99% 的人第一次写 MySQL 索引时，都会忽略这个关键点…… |
| 历史开场 | 演进过程、版本差异 | JDK 5 之前没有 ConcurrentHashMap，那并发读 Map 用什么？答案你可能想不到。 |

> 不必每篇都套模板，选最适合主题的那个。一篇文档只用一个开场，不要叠加。

### 开场避坑

- ❌ 大空话：「随着互联网的发展...」——读者直接关掉
- ❌ 声明式开头：「本文将介绍...」「本节讲解...」——毫无信息量
- ❌ 元信息标签：`> JDK 17+ | 新手友好 | 边学边练` ——像免责声明，劝退读者

## 节奏控制

**慢节奏**：核心原理、关键概念 — 自问自答、留有悬念、代码 + 解读配合

**快节奏**：配置步骤、列表内容、重复示例 — 简洁、直接、给代码就行

一篇 3000 字的文档，至少要有 2-3 次节奏切换。如果从头到尾都是同一个节奏，读者会睡着。

## 渐进式讲解

**漏斗式**：从大到小，逐步聚焦。大领域 → 核心问题 → 关键概念 → 细节展开 → 实践应用

**阶梯式**：每一步都有小成果。第一步跑起来（最简版本）→ 第二步加错误处理（完整版本）→ 第三步优化（调优版本）→ 第四步讲原理（原理版本）

**悬念递进**：层层剥开。提出问题 → 初步理解 → 发现新问题 → 深入理解 → 彻底掌握

## 表达技巧

**善用对比**：synchronized 的自动释放，是优点也是陷阱。不小心把 return 放在 try 里？锁早就丢了。

**善用转折**：大多数教程会告诉你：synchronized 可以解决线程安全问题。但我想告诉你：用错了 synchronized，问题更大。

**善用数字**：不要写「HashMap 在数据量大时性能会下降」，而要写「JDK 7 中，扩容时 rehash 可能导致 2^n 次重新分布」。

**善用类比**：B+ 树的查询，就像在字典里找字——先看拼音（定位索引），再翻到对应页码范围，每翻一页都是一次磁盘 I/O。

**善用管道标签**：`|synchronized|` 的锁升级是一个不可逆的过程。

## 字数与深度

| 类型 | 字数要求 | 目标 |
|---|---|---|
| 工具型 | 不限制 | 快速解决问题，准确和清晰是核心 |
| 普通文章 | `>= 1500` | 系统讲解知识点，让读者形成体系 |
| 原理类 | `>= 3000` | 彻底讲透原理，让读者从「会用」到「懂为什么」 |

普通文章必须覆盖的四步：这个技术怎么来的？为什么要这样设计？和同类方案比优劣？实际怎么用、坑在哪？

原理类在此基础上多两步：代价是什么？实际项目中有哪些应用？

> 不是字数竞赛，是防止文档停在「入门级」却自以为写透了。

## 结尾设计

让读者「意犹未尽」。推荐两种模式——抛出新问题引发下一步思考，或对比回顾引发新话题探索。

## 面试专题（可选）

本项目是 Java 面试指南，可以根据需要在文档中增加面试相关内容，增加实战感。

### 面试追问预判（推荐）

在知识点结尾，给出面试官可能的追问方向：

```markdown
## 总结

synchronized 的核心是对象锁，通过 Mark Word 记录锁状态。

**面试追问方向：**
- synchronized 锁升级的过程是什么？为什么不可逆？
- 偏向锁在什么情况下会撤销？
- 重量级锁的底层是什么？为什么 JDK 6 之后性能大幅提升？
```

### 面试官心理分析（可选）

在讲解技术点时，适当分析面试官的出题意图——面试官想看的不是答案本身，而是你对技术的理解深度和思维方式。

### 面试题剖析（可选）

遇到高频面试题，可以从三个层次展开：

- **表面层**：大部分人能答上来
- **深层**：优秀候选人能答上来
- **实战层**：让面试官眼前一亮的答案

### 面试场景模拟（可选）

设计「面试官问，你答」的模拟环节，让读者有身临其境的实战感。

### 使用原则

面试内容是加分项，不是必选项。技术原理讲清楚是前提，面试内容是锦上添花。如果为了加面试内容而影响原理讲解，删掉面试部分。
