---
description: Rspress 知识库文档编写规范 — 强调趣味性、渐进性、读者兴趣激发
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

> 核心目标：让每一篇文档都有「钩子」，让读者忍不住一口气读完

好的技术文档不是教科书，不是 API 手册，更不是面试题库的堆砌。它是**一场对话**——你在引导读者一步步揭开技术的面纱，在关键处停顿、抛出问题、给出悬念，直到最后让读者发出「原来如此」的感叹。

读者为什么会点开这篇文章？他可能是在准备面试，可能是在排查 bug，也可能只是随便逛逛。**你的任务是让他留下来，然后一直读下去。**

---

## 一、用「前 3 秒」抓住读者

### 五种开场模板

开场方式不需要面面俱到，记住这五种就够了：

| 模板 | 适用场景 | 示例 |
|---|---|---|
| 场景切入 | 痛点问题、bug 排查 | 「凌晨 2 点，你正准备上线新功能，数据库连接池突然耗尽……这是怎么回事？」 |
| 痛点共鸣 | 常见误区、认知陷阱 | 「synchronized 用了，代码还是有问题——问题到底在哪？」 |
| 核心追问 | 原理讲解、高频面试题 | 「HashMap.get() 的时候，JVM 到底做了什么？追问下去，十个人里有九个答不上来。」 |
| 反差对比 | 打破固有认知 | 「很多人说 synchronized 性能很差。但 JDK 6 之后，JVM 团队花了整整三年优化它。」 |
| 悬念制造 | 引入新概念 | 「HashMap 在 JDK 8 之后，永远不会出现死循环了。这是真的吗？」 |

### 开场避坑

| 避坑 | 说明 |
|---|---|
| ❌ 大空话 | 「随着互联网的发展...」——读者直接关掉 |
| ❌ 声明式开头 | 「本文将介绍...」「本节讲解...」——毫无信息量 |
| ❌ 元信息标签 | `> JDK 17+ \| 新手友好 \| 边学边练` ——像免责声明，劝退读者 |

---

## 二、节奏控制：让读者「一口气读完」

### 两种节奏交替

| 节奏 | 适用 | 特点 |
|---|---|---|
| **慢节奏** | 核心原理、关键概念 | 自问自答、留有悬念、代码 + 解读配合 |
| **快节奏** | 配置步骤、列表内容、重复示例 | 简洁、直接、给代码就行 |

### 起伏设计原则

```
开场（悬念）→ 基础概念（慢）→ 快速上手（快）→ 深入原理（慢）→ 实战应用（快）→ 留白思考（悬念）
```

一篇 3000 字的文档，至少要有 2-3 次节奏切换。**如果从头到尾都是同一个节奏，读者会睡着。**

---

## 三、渐进式讲解：从「不知道」到「恍然大悟」

### 三种核心方式

**漏斗式**：从大到小，逐步聚焦。

```
大领域 → 核心问题 → 关键概念 → 细节展开 → 实践应用
```

**阶梯式**：每一步都有小成果。

```
第一步：让代码跑起来（最简版本）
第二步：加上错误处理（完整版本）
第三步：优化性能（调优版本）
第四步：解释为什么这样设计（原理版本）
```

**悬念递进**：层层剥开。

```
提出问题 → 初步理解 → 发现新问题 → 深入理解 → 彻底掌握
```

---

## 四、表达技巧：让文字「有性格」

### 善用对比

```markdown
别小看这个区别——

synchronized 的自动释放，是优点也是陷阱。
你不小心把 return 放在 try 里？锁早就丢了。
```

### 善用类比

```markdown
B+ 树的查询过程，就像在字典里找字：
先看拼音（定位索引），再翻到对应页码范围，最后找到具体的字。
每翻一页，都是一次磁盘 I/O。
```

### 善用转折——打破读者预期

```markdown
大多数教程会告诉你：synchronized 可以解决线程安全问题。

但我想告诉你：**用错了 synchronized，问题更大。**

比如在循环里加锁，每次都要竞争锁——
你以为在保护共享资源，其实是在让线程排队等锁。
```

### 善用数字——让描述更精准

```markdown
# 不要
"HashMap 在数据量大时性能会下降"

# 要
"HashMap 在 JDK 7 中，扩容时 rehash 会重新计算每个元素的哈希位置，
在极端情况下，可能导致 2^n 次重新分布"
```

### 善用引语

```markdown
> "我不在乎代码能不能跑，我在乎的是未来的自己能看懂。" —— 某位不愿透露姓名的开发者
```

### 善用内联标签——让句子有层次感

两种方式：**管道标签**和 **Badge 组件**，前者适合快速突出名词概念，后者适合标注版本号、协议名、状态等分类语义。

````markdown
|synchronized| 的锁升级是一个**不可逆**的过程，
从偏向锁一路升级到重量级锁后，锁释放了，再次加锁时也不会回到初始状态。
````

````mdx
import { Badge } from '@rspress/core/theme';

|synchronized| 的锁升级 <Badge text="JDK 6+" type="tip" /> 是一个**不可逆**的过程。
|JDK 7| <Badge text="JDK 7" type="warning" /> 用头插法，可能产生环形链表；
|JDK 8| <Badge text="JDK 8" type="tip" /> 改用尾插法，解决了这个问题。
````

> **判断**：能用 Badge 说清楚的，就不用容器。容器适合需要展开说明的复杂背景或风险。

---

## 五、代码呈现：让代码「会说话」

### 渐进式展示：从坏到好

```markdown
## 这样的 HashMap 用法，有问题

```java
Map map = new HashMap();
map.put("key", "value");
String value = (String) map.get("key");  // 类型安全全丢了
```

问题在哪？JDK 5 之后，我们有了更好的选择。

## 改进：用泛型约束类型

```java
Map<String, String> map = new HashMap<>();
String value = map.get("key");  // 不需要强转了
```

**但还有优化的空间。** `new HashMap<>()` 默认容量是多少？答案是 16，负载因子 0.75。

## 最终形态：考虑性能

```java
// 预估容量 = 期望元素数 / 负载因子 + 1
Map<String, String> map = new HashMap<>(32);
```

三个版本的演变，就是 HashMap 使用进化的缩影。
```

### 代码注释：告诉「为什么」而不是「是什么」

```markdown
# 不要
map.get(key);  // 根据 key 获取 value

# 要
Object value = map.get(key);
// 注意：如果 key 不存在，返回 null
// 如果允许 null 值，用 map.getOrDefault(key, defaultValue)
```

---

## 六、格式规范与结尾设计

### 段落格式：句间不空行

同一段落内的多句话**不要用空行分隔**，保持阅读节奏流畅。空行仅用于**分隔不同段落**、**标题前后**、**代码块前后**。

### 待办清单：使用 GFM 语法

```mdx
- [ ] 知道压力面是正常的，不是自己不行
- [ ] 心态从「被考核」切换到「技术讨论」
- [x] 准备了「承认错误」的话术
```

> 不得使用 Unicode 符号 `☐` / `☑` 模拟复选框，在部分浏览器中会渲染为空白。

### 结尾：让读者「意犹未尽」

结尾不是总结，是**下一次探索的起点**。推荐两种模式：

**模式一：抛出新问题**

```markdown
synchronized 的锁升级是一个不可逆的过程。

但如果一个对象从偏向锁一路升级到重量级锁后，
锁释放了，再次加锁时，会从哪个状态开始？
这个问题，值得你去看一下 HotSpot 源码。
```

**模式二：对比回顾，引发下一步**

```markdown
我们讲了 synchronized 的锁升级，你知道 Lock 有什么 synchronized 没有的能力吗？
公平锁、可中断获取锁、多条件变量……
下一节，我们来聊聊 Lock 的高级特性。
```

---

## 七、中文字数下限：按文档类型设定门槛

### 文档三分法

| 类型 | 字数要求 | 目标 |
|---|---|---|
| 工具型 | 不限制 | 快速解决问题，准确和清晰是核心 |
| 普通文章 | `>= 1500` | 系统讲解知识点，让读者形成体系 |
| 原理类 | `>= 3000` | 彻底讲透原理，让读者从「会用」到「懂为什么」 |

### 各类型的四步框架

普通文章必须覆盖的四步：

```
这个东西怎么来的？（起源与动机）
为什么要这样设计？（核心矛盾与权衡）
和同类方案比有什么优劣？（对比与适用场景）
实际怎么用，坑在哪？（实践与边界）
```

原理类在此基础上多两步：

```
代价是什么？（每一种设计都有代价，原理类必须讲清楚）
实际项目中有哪些应用？（原理到实践的桥梁）
```

### 为什么原理类是 3000

1500 字能讲清楚一个知识点的全貌，但往往跳过了「代价」和「实际应用」。原理类如果篇幅不够，通常意味着：跳过了「为什么这样设计」、缺少对比与适用场景分析、代码贴了一堆但没有解释背后的逻辑、没有讲清楚「代价」是什么。

> 本规则的核心目的：**工具型不废话，普通型讲体系，原理型讲透彻。** 不是字数竞赛，是防止文档停在「入门级」却自以为写透了。
