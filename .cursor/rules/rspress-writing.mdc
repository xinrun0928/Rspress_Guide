---
description: Java 面试复习文档编写规范 — 写作风格
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

# 写作风格规则

> 好的面试复习文档，不是 API 参数表，不是概念堆砌，更不是「复制官网再换个说法」。它应该像一场高质量的技术面试：从真实问题出发，逐层拆解设计背景、核心原理、关键权衡与落地方案。读者读完之后，不只是知道「怎么答」，更知道「为什么这么答、什么时候不该这么答、面试官追问时该怎么接」。真正优秀的复习文档，最终要帮助读者建立自己的技术判断力。

## 一、开场设计

大多数读者会在前 30 秒决定这篇文章值不值得继续读。所以开场不要急着「介绍本文内容」，而是先把读者拉进面试情境里。

### 开场的核心目标

好的开场至少要做到下面三件事中的两件：

1. 让读者产生代入感——「这题我被问过」
2. 让读者意识到问题的深度——「原来背后还有这么多」
3. 让读者愿意继续往下读——「终于有人讲清楚了」

### 常见开场模板

| 模板 | 适用场景 | ���例 |
|---|---|---|
| 场景切入 | 线上踩坑、高频原题 | 面试官问：「HashMap 是线程安全的吗？」你脱口而出「不是」——然后面试官紧接着追问「那 JDK7 呢？JDK8 呢？为什么？」瞬间答不上来。 |
| 痛点共鸣 | 常见误区、混淆概念 | 「synchronized 和 ReentrantLock 都能加锁，所以用哪个都一样」——这种理解，在 P6 面试里是致命的。 |
| 核心追问 | 原理类、底层机制 | 面试官问：「volatile 是怎么保证可见性的？」你说「通过内存屏障」——然后面试官追问「内存屏障在 x86 和 ARM 上实现一样吗？」你沉默了。 |
| 反差对比 | 打破认知、版本差异 | 很多人以为「JVM 调优就是改 Xmx」——但真正影响延迟的，是 GC 算法的选择和年轻代/老年代的比例，而不是内存大小。 |
| 悬念制造 | 引入新概念、复杂设计 | 「这道题 10 个面试者里有 8 个答不完整，但它其实是 JDK 面试里性价比最高的题目之一。」 |
| 一行代码开场 | 参数分析、原理讲解 | `synchronized (this) { ... }` —— 你以为这段代码只是「加了锁」，但 JVM 可能在背后完成了偏向锁撤销、轻量级锁膨胀、monitorenter 三部曲。 |
| 数字开场 | 性能分析、复杂度问题 | 90% 的多线程面试题，考察的根本不是「会不会用」，而是「理不理解底层」。 |
| 历史演进 | 版本差异、技术演化 | JDK7 和 JDK8 的 HashMap 有什么区别？「红黑树」三个字只是开始——真正的问题是：为什么是红黑树而不是 AVL 树？为什么阈值是 8？ |

> 不必每篇都套模板，选最适合主题的那个。一篇文档只用一个开场，不要叠加。

### 开场避坑

**不要这样写**：

- ❌ 随着互联网的发展……
- ❌ 本文将介绍……
- ❌ 本节主要讲解……
- ❌ 在日常开发中，我们经常会……
- ❌ 大家都知道……

这些开场没有信息量，也没有情绪张力。直接进入问题、冲突、错误结论或反直觉事实。因为读者不是来「看目录」的，而是来「搞定面试」的。

## 二、文章结构设计

面试复习文档最大的失败，不是写错，而是写浅。一篇文章如果没有深度追问和陷阱设计，读者面试时问到第二层就卡住了。

### 推荐结构：漏斗式展开

从「大问题」逐步聚焦到「小问题」，从「是什么」到「为什么」到「怎么答」。推荐顺序：

1. 面试官最关心的 3 个问题（快速自测）
2. 核心原理：为什么这样设计
3. 源码解析：关键代码逐行解读
4. 高频追问��：面试官会怎么挖坑
5. 常见错误与陷阱
6. 对比总结表
7. 加分回答（超出预期的深度）

### 推荐结构：阶梯式推进

让读者每一节都有「又多学会了一道题」的感觉。推荐顺序：

1. 表层答案：能回答「是什么」
2. 深层原理：能回答「为什么」
3. 追问扩展：能回答「JDK7 vs JDK8」「还有什么替代方案」
4. 实战应用：能回答「项目里怎么用的」「踩过什么坑」
5. 加分项：能展示超出预期的技术深度

## 三、节奏控制

一篇复习文档如果从头到尾都是「概念解释」，读者读完只会背概念，一被追问就露馅。

### 慢节奏适合讲什么

适合：核心原理、源码解读、追问链设计、设计权衡分析。

慢节奏要做三件事：多追问、多拆解、多解释「为什么」。

例如，不要只写「HashMap 扩容时 rehash」，而要继续追问：为什么 JDK8 优化了 rehash？JDK7 的 rehash 有什么问题？扩容因子 0.75 是怎么算出来的？为什么必须是 2 的幂次？

### 快节奏适合讲什么

适合：命令说明、参数列表、快速对比、结论总结。这些部分不要啰嗦，用户要的是快速获取信息。

### 节奏切换建议

一篇 3000 字左右的文章，至少应该有 2~3 次节奏变化。例如：

- 开场：快节奏，引发兴趣
- 原理：慢节奏，深入讲解
- 源码：慢节奏，逐行分析
- 追问：快节奏，直击要害
- 总结：快节奏，收束对比

## 四、表达技巧

### 1. 多用对比，而不是单独介绍

不要只说一个技术好。一定要回答：它比什么好？好在哪里？代价是什么？什么场景下反而更差？

例如：讲 ConcurrentHashMap，不能只讲 JDK8 的 CAS + synchronized，必须回答「JDK7 的分段锁和 JDK8 的 CAS + synchronized 哪个更好？为什么 JDK8 要改？」

### 2. 多用数字，而不是模糊表达

不要写「数据量大时会变慢」「并发高时可能有问题」。

应该写「当链表长度超过 8 时会转为红黑树」「在 10 万并发下，synchronized 的自旋次数会影响 20%~30% 的性能」。数字会让结论更可信，也让面试官觉得你有实战经验。

### 3. 多用转折，增强阅读张力

例如：

- 很多人以为……但真正的问题是……
- 表面上看这是线程安全问题，但实际是 JVM 内存模型的可见性问题
- 大多数候选人能回答第一层，但第二层就开始含糊了
- 你可能知道 synchronized 能加锁，但你知道它在 JDK8 里经历了哪些优化吗？

### 4. 多用类比，但不要滥用

复杂概念可以借助类比帮助理解。例如：

- AQS 像停车场入口的闸机：state 是剩余车位，队列是排队车辆
- volatile 内存屏障像超市的「一手交钱一手交货」规则：不允许先结账再拿货
- synchronized 锁升级像追女生的四个阶段：无感 → 暧昧 → 公开 → 稳定交往

但类比只能帮助入门，后面必须回到真实机制和源码。

### 5. 善用追问链设计

面试官不会只问一层。每一道高频题都要设计 2-3 层追问链：

> **第一层**：HashMap 的 put 流程是怎样的？
> **第二层**：为什么容量必须是 2 的幂次？
> **第三层**：JDK8 为什么引入红黑树？阈值 8 是怎么来的？
> **第四层**：如果 JDK7 和 JDK8 的 put 流程有区别，区别在哪？

## 五、代码与源码规范

代码示例不是越多越好，而是越「能说明面试考点」越好。

### 好代码示例的标准

1. 贴近真实面试场景（不是企业级业务代码）
2. 能直接回答「这道题怎么答」
3. 重点代码有逐行解读
4. 能展示追问链的答案

### 推荐代码组织方式

**第一段：核心源码**——贴出 JDK 源码的关键片段，逐行解读。

**第二段：面试回答模板**——「如果面试官问 XXX，就这么回答……」

**第三段：错误示例**——「很多人会这么答，但这三个地方说错了……」

## 六、原理类文章必须回答的核心问题

原理类面试复习文章不是介绍某个组件怎么用，而是解释为什么要这样设计。必须回答：

1. **它解决了什么问题？** 没有问题背景的原理，是空中楼阁。
2. **为什么不用更简单的方案？** 复杂方案一定有代价。
3. **它的代价是什么？** 任何设计都有 trade-off。
4. **什么场景下不应该用？** 真正理解一个技术的边界，比会用它更重要。

## 七、真实面试视角

面试复习文档的定位不是「技术百科」，而是「面试官思维模拟器」。所以文档必须模拟面试官的真实提问方式。

推荐加入的内容：

- 面试官的真实追问路径
- 候选人常见的 2-3 个错误回答
- P5/P6/P7 不同级别的回答深度差异
- 面试官想听到的「加分回答」是什么

推荐句式：

- 如果是 P5 面试，能回答到第一层就够了
- 如果是 P6 面试，面试官一定会追问第二层
- 很多候选人卡在这个问题上，是因为没有理解……

## 八、字数与深度要求

| 类型 | 字数建议 | 目标 |
|---|---|---|
| 工具型（命令/配置） | 不限制 | 快速查找，准确清晰 |
| 普通面试题 | `>= 1500` | 建立完整答题体系 |
| 原理类 | `>= 3000` | 讲透底层机制与设计逻辑 |
| 系统设计类 | `>= 2000` | 讲清 trade-off、适用场景与边界 |

### 普通面试题至少回答四个问题

1. 这道题面试官想问什么？
2. 表层答案和深层原理分别是什么？
3. 和同类方案相比有什么优劣？
4. 面试官会怎么追问？

### 原理类文章再多回答两个问题

5. 它的代价（trade-off）是什么？
6. JDK 不同版本之间有什么变化？

### 系统设计类文章重点回答三个问题

1. 解决了什么问题？
2. 不用它的代价是什么？
3. 什么场景下不应该用？

> 不是字数竞赛，是防止文档停在「背答案」却自以为准备充分了。

## 九、结尾设计

不要用「本文到这里就结束了」这种收尾。好的结尾应该让读者继续思考，或形成闭环。推荐三种方式：

**方式一：抛出新问题**。例如：「既然 synchronized 已经能解决并发问题，为什么还需要 ReentrantLock？」

**方式二：延伸对比**。例如：「HashMap 解决了查询问题，但如果业务需要保证数据不丢失，仅靠 HashMap 够吗？」

**方式三：回到开场**。开头提到面试卡壳的场景，结尾回到那个问题，告诉读者「现在你应该能回答了」。

## 十、最终判断标准

写完一篇文章之后，不要先问「我写得全不全」，而要问：

1. 读者读完，能不能自信回答面试官的第一层追问？
2. 读者被追问到第二层、第三层时，会不会答不上来？
3. 读者看到同类题目，能不能自己举一反三？
4. 读者是否知道这个技术在什么场景下会「出事」？

如果答案是「能」，这份复习文档才算真的写好了。
