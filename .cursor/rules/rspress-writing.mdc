---
description: Rspress 知识库文档编写规范 — 强调趣味性、渐进性、读者兴趣激发
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

> 核心目标：让每一篇文档都有「钩子」，让读者忍不住一口气读完

## 核心理念：从「灌知识」到「点燃好奇心」

好的技术文档不是教科书，不是 API 手册，更不是面试题库的堆砌。

它是**一场对话**——你在引导读者一步步揭开技术的面纱，在关键处停顿、抛出问题、给出悬念，直到最后让读者发出「原来如此」的感叹。

读者为什么会点开这篇文章？他可能是在准备面试，可能是在排查 bug，也可能只是随便逛逛。**你的任务是让他留下来，然后一直读下去。**

---

## 一、开场：用「前 3 秒」抓住读者

读者的时间有限，耐心更有限。如果开场无聊，他 3 秒就关掉了。

### 八种开场方式

#### 1. 场景切入——让读者「身临其境」

```markdown
凌晨 2 点，你正准备上线新功能，数据库连接池突然耗尽。
日志里全是 `Connection timeout`，但你没改任何配置。
这是怎么回事？
```

> **为什么有效**：把读者拉进具体场景，他会产生「这就是我」的代入感。

#### 2. 痛点共鸣——说出读者想说但没说出口的话

```markdown
你一定遇到过这种情况：synchronized 用了，代码还是有问题。
加了 volatile，线程 A 修改的值，线程 B 还是读不到。
——问题到底在哪？
```

> **为什么有效**：读者会觉得「你懂我」，从而产生信任感。

#### 3. 核心追问——用问题撕开知识的面纱

```markdown
# HashMap.get() 的时候，JVM 到底做了什么？

大多数人只会说「调用 hashCode()，然后找到对应位置」。
但追问下去，十个人里有九个答不上来。
```

> **为什么有效**：人天生讨厌「不知道」的感觉，求知欲会驱使他读下去。

#### 4. 反差对比——打破固有认知

```markdown
很多人说：synchronized 性能很差，能不用就不用。

但 JDK 6 之后，JVM 团队花了整整三年优化它。
现在的 synchronized，性能已经可以和 Lock 持平。
```

> **为什么有效**：反差制造认知冲突，而冲突是注意力最强的时刻。

#### 5. 数字锚定——用数据说话

```markdown
90% 的 MySQL 慢查询，归结于这三个原因。

掌握它们，能让你的查询从 10 秒降到 50 毫秒。
```

> **为什么有效**：具体的数字比模糊的描述更有说服力。

#### 6. 故事开场——给技术一个「身世」

```markdown
2014 年，James Gosling 在一次访谈中回忆：
Java 最初的名字是「Green」，目标是为消费电子设备编程。
谁能想到，这个为微波炉设计的语言，后来统治了企业级开发？
```

> **为什么有效**：故事是人类理解世界最原始的方式。

#### 7. 悬念制造——先给结论，再讲原因

```markdown
# HashMap 在 JDK 8 之后，永远不会出现死循环了。

这是真的吗？
```

> **为什么有效**：人天生讨厌「悬而未决」，一定要知道答案。

#### 8. 快速上手——先让读者尝到甜头

```markdown
先看效果：

没加索引：查询 100 万条数据，用了 8.2 秒
加了索引：同样查询，只用了 12 毫秒

怎么做到的？往下看。
```

> **为什么有效**：先给甜头，让读者觉得「值」，他才愿意继续投入时间。

### 开场避坑

| 避坑 | 说明 |
|------|------|
| ❌ 大空话 | 「随着互联网的发展...」「在当今社会...」—— 读者直接关掉 |
| ❌ 声明式开头 | 「本文将介绍...」「本节讲解...」—— 毫无信息量 |
| ❌ 长篇铺垫 | 铺垫三段才进正题——消耗耐心 |
| ❌ 元信息标签 | `> JDK 17+ \| 新手友好 \| 边学边练` —— 像免责声明，劝退读者 |

---

## 二、节奏控制：让读者「一口气读完」

读者阅读的过程就像爬山——你需要控制坡度，让他不觉得累，同时又不断有新风景。

### 慢节奏：核心原理与关键概念

适用于：第一次介绍重要概念、需要深度理解的地方。

```markdown
## synchronized 的锁，到底锁的是什么？

不是锁代码，是锁**对象**。

当线程 A 进入 synchronized 代码块时，它需要在对象头里「做标记」。
这个标记，表明：「这个对象正在被我占用，你们等等」。

但这里有个微妙的地方——synchronized 加在**方法**上和加在**代码块**上，
锁的东西是不一样的，你知道区别在哪吗？
```

> **特点**：自问自答、留有悬念、代码 + 解读配合。

### 快节奏：配置步骤与重复性内容

适用于：环境搭建、配置说明、列表类内容。

```markdown
## MySQL 索引创建

```sql
-- 单列索引
ALTER TABLE orders ADD INDEX idx_user_id (user_id);

-- 联合索引（注意字段顺序）
ALTER TABLE orders ADD INDEX idx_user_status (user_id, status);

-- 查看索引
SHOW INDEX FROM orders;
```
```

> **特点**：简洁、直接、给代码就行。

### 起伏设计原则

```
开场（悬念）→ 基础概念（慢）→ 快速上手（快）→ 深入原理（慢）→ 实战应用（快）→ 留白思考（悬念）
```

一篇 3000 字的文档，至少要有 2-3 次节奏切换。**如果从头到尾都是同一个节奏，读者会睡着。**

---

## 三、渐进式讲解：从「不知道」到「恍然大悟」

这是最核心的写作技巧：**永远不要一下子把所有东西扔给读者。**

### 漏斗式：从大到小，逐步聚焦

```
大领域 → 核心问题 → 关键概念 → 细节展开 → 实践应用

例如讲 HashMap：
1. HashMap 是什么？（键值对存储）
2. 为什么需要它？（数组查询快但插入慢，链表插入快但查询慢）
3. HashMap 怎么解决这个矛盾？（哈希 + 数组 + 链表）
4. JDK 8 又做了什么优化？（链表转红黑树）
5. 实际怎么用？（代码示例 + 注意事项）
```

### 阶梯式：每一步都有小成果

```
第一步：让代码跑起来（最简版本）
第二步：加上错误处理（完整版本）
第三步：优化性能（调优版本）
第四步：解释为什么这样设计（原理版本）
```

每上一个台阶，读者都有「我做到了」的成就感。

### 悬念递进：层层剥开

```
提出问题 → 初步理解 → 发现新问题 → 深入理解 → 彻底掌握

例如讲 volatile：
1. 可见性是什么？（线程 A 修改，线程 B 看不到）
2. synchronized 能解决吗？（能，但太重）
3. volatile 呢？（轻量级解决方案）
4. volatile 底层怎么实现的？（内存屏障）
5. volatile 能替代锁吗？（不能，有些场景不适用）
```

---

## 四、表达技巧：让文字「有性格」

### 善用对比——让差异一目了然

```markdown
# synchronized vs Lock

| 特性 | synchronized | Lock |
|------|-------------|------|
| 获得锁失败 | 阻塞等待 | 可选择超时/放弃 |
| 释放锁 | 自动释放 | 必须手动释放 |
| 公平锁 | 否 | 可配置 |
| 注解支持 | 无 | 有 @Locked |
```

```markdown
别小看这个区别——

synchronized 的自动释放，是优点也是陷阱。
你不小心把 return 放在 try 里？锁早就丢了。
```

### 善用类比——让抽象变具体

```markdown
B+ 树的查询过程，就像在字典里找字：

1. 先看拼音（第 1 层索引）
2. 再翻到对应页码范围（第 2 层索引）
3. 最后找到具体的字（叶子节点）

每翻一页，都是一次磁盘 I/O。
所以树越深，查询越慢——这也是为什么 MySQL 要控制 B+ 树的深度。
```

### 善用转折——打破读者预期

```markdown
大多数教程会告诉你：synchronized 可以解决线程安全问题。

但我想告诉你：**用错了 synchronized，问题更大。**

比如在循环里加锁，每次都要竞争锁——
你以为在保护共享资源，其实是在让线程排队等锁。
```

### 善用数字——让描述更精准

```markdown
# 不要
"HashMap 在数据量大时性能会下降"

# 要
"HashMap 在 JDK 7 中，扩容时 rehash 会重新计算每个元素的哈希位置，
在极端情况下，可能导致 2^n 次重新分布"
```

### 善用引语——增加人情味

```markdown
> "我不在乎代码能不能跑，我在乎的是未来的自己能看懂。" —— 某位不愿透露姓名的开发者

好的命名和注释，不是给机器看的，是给未来的自己留的退路。
```

### 善用内联标签——让句子有层次感

正文句子中，关键概念、重点强调可以用内联标签突出，制造视觉层次。两种常用方式：

#### 方式一：`|...|` 管道标签

````markdown
|synchronized| 的锁升级是一个**不可逆**的过程，
从偏向锁一路升级到重量级锁后，锁释放了，再次加锁时也不会回到初始状态。

|HashMap| 不是线程安全的 —— 这句话你可能听过，
但 **JDK 7 和 JDK 8 的原因并不一样**，你知道区别吗？

大多数人会说「Dubbo 性能好」，但真正决定性能的是 **这三件事**：
网络传输、序列化方式、线程模型。
````

#### 方式二：Badge 组件

适合标注**技术栈、版本号、协议名**等具有分类语义的内容：

````mdx
import { Badge } from '@rspress/core/theme';

|synchronized| 的锁升级 <Badge text="JDK 6+" type="tip" /> 是一个**不可逆**的过程。

|HashMap| 不是线程安全的 <Badge text="JDK 7" type="warning" /> <Badge text="JDK 8" type="info" />。

Dubbo 支持 Triple 协议 <Badge text="gRPC" type="tip" outline />，性能优异。
````

> **使用原则**：`|...|` 适合纯文本段落中快速突出名词概念；Badge 适合带有版本、协议、状态等语义标签的场景。两者不要叠加在同一词上。加粗（`**...**`）用于强调形容词或判断，与前两者形成互补。

---

## 五、代码呈现：让代码「会说话」

### 渐进式展示：从坏到好

```markdown
## 这样的 HashMap 用法，有问题

```java
Map map = new HashMap();
map.put("key", "value");
String value = (String) map.get("key");
```

**问题在哪？**

类型安全全部丢掉了，运行时才发现类型错误。
JDK 5 之后，我们有了更好的选择。
```

```markdown
## 改进：用泛型约束类型

```java
Map<String, String> map = new HashMap<>();
map.put("key", "value");
String value = map.get("key");  // 不需要强转了
```

**但还有优化的空间。**

你知道 `new HashMap<>()` 默认容量是多少吗？
答案是 16，负载因子是 0.75。
这意味着什么？
```

```markdown
## 最终形态：考虑性能

```java
// 预估容量 = 期望元素数 / 负载因子 + 1
// 减少扩容次数，提升性能
Map<String, String> map = new HashMap<>(32);
```

三个版本的演变，就是 HashMap 使用进化的缩影。
```

### 代码注释：告诉「为什么」而不是「是什么」

```markdown
# 不要：注释在解释「是什么」
```java
map.get(key);  // 根据 key 获取 value
```

# 要：注释在解释「为什么」和「例外情况」
```java
Object value = map.get(key);
// 注意：如果 key 不存在，返回 null
// 如果允许 null 值，用 map.getOrDefault(key, defaultValue)
```
```

---

## 七、段落格式规范：句间不空行

同一段落内的多句话**不要用空行分隔**，保持阅读节奏流畅：

```markdown
# 正确：句间紧凑
Dubbo 的架构设计非常精妙，它采用了分层架构，每一层都有明确的职责。我们先从整体看：

# 错误：句间有空行
Dubbo 的架构设计非常精妙，它采用了分层架构，每一层都有明确的职责。
（空行）
我们先从整体看：
```

空行仅用于**分隔不同段落**、**标题前后**、**代码块前后**。

---

## 七、待办清单格式规范

待办清单应使用 **GFM（GitHub Flavored Markdown）任务列表** 语法，`- [ ]` 表示未完成，`- [x]` 表示已完成。不得使用 Unicode 符号 `☐` / `☑` 模拟复选框，因其依赖字体支持，在部分浏览器中可能渲染为空白。

````mdx
- [ ] 知道压力面是正常的，不是自己不行
- [ ] 心态从「被考核」切换到「技术讨论」
- [ ] 准备了「确认问题」的句式
- [x] 准备了「承认错误」的话术
````

> 以下写法**不可用**，`☐` 在许多系统中无法正常渲染，会显示为空白方框：

````mdx
| 检查项 | 状态 |
| --- | --- |
| 知道压力面是正常的，不是自己不行 | ☐ |  <- 不要用
````

---

## 八、结尾设计：让读者「意犹未尽」

结尾不是总结，是**下一次探索的起点**。

### 类型一：抛出新问题

```markdown
## 留给你的问题

synchronized 的锁升级（无锁 → 偏向锁 → 轻量级锁 → 重量级锁），
是一个不可逆的过程。

但如果一个对象从偏向锁一路升级到重量级锁后，
锁释放了，再次加锁时，会从哪个状态开始？

这个问题，值得你去看一下 HotSpot 源码。
```

### 类型二：给行动清单

```markdown
## 下一步，你可以

- [ ] 用 `jstack` 查看自己项目里的线程状态
- [ ] 分析哪些 synchronized 可以换成 ReentrantLock
- [ ] 用 Arthas 监控锁竞争情况
```

### 类型三：对比回顾，引发思考

```markdown
## 最后留个思考题

我们讲了 synchronized 的锁升级，你知道 Lock 有什么 synchronized 没有的能力吗？

公平锁、可中断获取锁、多条件变量……

下一节，我们来聊聊 Lock 的高级特性。
```

### 类型四：金句升华

```markdown
最后，送你一句话：

> "最好的代码注释，是让未来的自己感谢现在的自己。"

共勉。
```
