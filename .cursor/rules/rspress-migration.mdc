# MDX 转换规则：从外部项目迁移到 Rspress

本规则描述将 MDX 文件从外部项目批量迁移到 Rspress 时，内容转换和样式增强的统一规范。适用于 `docs/` 下所有 `.mdx` 文件的批量转换。

---

## 一、转换原则

| 原则 | 说明 |
|---|---|
| **内容优先** | 转换以丰富内容为核心，样式服务于内容 |
| **渐进增强** | 先保留原意，再逐步提升表达层次 |
| **层次感** | 通过 Rspress 原生组件体现内容的重要程度 |
| **可运行性** | 代码块必须标注正确语言，示例必须可运行 |
| **不删内容** | 不删除任何实质内容，只调整表达方式 |

---

## 二、文本块（Plain Text in Code Block）转换

### 问题识别

大量文件使用 ```` ``` ```` 包裹纯文本内容（如配置步骤、流程说明、ASCII 图），导致：

- 无法语法高亮，阅读体验差
- 语义不清晰，读者无法快速定位关键信息
- 排版单调，缺乏视觉层次

### 转换规则

#### 2.1 ASCII 架构图 → `:::details` + 结构化描述

**识别特征**：

- 包含 `┌─┬─┐`、`│`、`└─┴─┘` 等 Box Drawing 字符
- 包含 `├─┼─┤`、`│   │` 等树状结构
- 包含 `→`、`──▶` 等流程箭头

**转换方式**：

````mdx
# 转换前
```
┌─────────────────────────────────────────────────────────────┐
│                    Logstash Pipeline                        │
│   Input          Filter              Output                  │
│  ┌─────┐       ┌─────┐            ┌─────┐                  │
└─────────────────────────────────────────────────────────────┘
```

# 转换后
:::details 点击展开查看 Pipeline 架构图
```
┌─────────────────────────────────────────────────────────────┐
│                    Logstash Pipeline                        │
│   Input          Filter              Output                  │
│  ┌─────┐       ┌─────┐            ┌─────┐                  │
└─────────────────────────────────────────────────────────────┘
```
:::
````

> **判断标准**：如果图的上方或下方已有文字描述核心内容，用 `:::details` 包裹并添加有意义的标题；如果图是唯一的说明，需要**展开描述**替代。

#### 2.2 步骤列表 → `:::tip` 容器

**识别特征**：

- 单层或多层 `└─→` 树状结构
- `1. 创建  2. 配置  3. 保存` 形式的步骤列表
- 纯文本描述的操作流程

**转换方式**：

````mdx
# 转换前
```
创建阈值告警步骤：

1. 创建规则
   └─→ Stack Management → Rules and Connectors → Create rule

2. 选择告警类型
   └─→ Threshold

3. 配置条件
   └─→ 索引、字段、运算符、阈值
```

# 转换后
:::tip 创建阈值告警的步骤
1. **创建规则**：Stack Management → Rules and Connectors → Create rule
2. **选择告警类型**：Threshold
3. **配置条件**：索引、字段、运算符、阈值
:::

# 或者多层级步骤，用有序列表替代
1. 创建规则
   └─→ Stack Management → Rules and Connectors → Create rule
2. 选择告警类型
   └─→ Threshold
3. 配置条件
   └─→ 索引、字段、运算符、阈值
````

> **注意**：如果步骤包含**代码路径**（如 `Stack Management → Rules → Create`），保留原始格式；如果只是**操作说明**，转换为有序列表。

#### 2.3 流程图 → `:::tip` 或 `:::info` 容器

**识别特征**：

- 多段 `→` 连接的流程（如 `指标监控 → 条件判断 → 发送通知`）
- 清晰的单向或多向流程

**转换方式**：

````mdx
# 转换前
```
告警流程：

指标监控 → 条件判断 → 触发阈值 → 发送通知 → 响应处理

例如：
CPU > 80% → 持续 5 分钟 → 发送邮件 → 运维处理
```

# 转换后
:::info 告警流程
指标监控 → 条件判断 → 触发阈值 → 发送通知 → 响应处理

**示例**：CPU > 80% → 持续 5 分钟 → 发送邮件 → 运维处理
:::
````

#### 2.4 无意义的纯文本块 → 删除或合并

如果一个纯文本代码块仅包含**装饰性内容**（如重复的分隔线、空表格骨架），直接删除：

````mdx
# 转换前
```
┌─────────────────────────┐
│                       │
└─────────────────────────┘
```

# 转换后
删除该代码块，不保留空架子。
````

---

## 三、代码块语言修复

### 问题识别

大量代码块使用 `java` 作为语言标识符，但实际内容是：

- Logstash 配置文件（DSL）
- Kibana/Elasticsearch JSON 配置
- YAML 配置
- Shell/Bash 脚本
- SQL 语句

### 转换规则

| 实际内容 | 应使用语言标识符 |
|---|---|
| Logstash 配置（`input {}`, `filter {}`, `output {}`） | `logstash` 或删除语言标识 |
| Kibana/ES JSON 配置 | `json` |
| YAML 配置文件（`key: value` 格式） | `yaml` |
| Shell/Bash 命令和脚本 | `bash` |
| SQL 查询语句 | `sql` |
| 真正的 Java 代码（`public class`, `System.out`） | `java` |
| Dockerfile 内容 | `dockerfile` |
| XML 配置 | `xml` |
| Properties 文件 | `properties` |

### 判断逻辑

````mdx
# Logstash 判断
# 如果包含 input { / filter { / output { → logstash

# YAML 判断
# 如果行以 "key: value" 格式缩进出现 → yaml
# 如果包含 Spring Boot 配置结构 → yaml

# JSON 判断
# 如果内容以 { 开头且有 "key": "value" 格式 → json

# Shell 判断
# 如果包含 curl / bin/logstash / chmod / #!/bin/bash → bash
````

---

## 四、容器增强规范

### 4.1 关键结论 → `:::tip`

原文中有「核心要点」「重点」「建议」等表述：

````mdx
# 转换前
**关键点：Slave 是被动接收的，不是主动去拉。**
Master 推多少，Slave 收多少。

# 转换后
:::tip 关键点
Slave 是被动接收的，不是主动去拉。Master 推多少，Slave 收多少。
:::
````

### 4.2 注意事项 → `:::warning`

原文中有「注意」「不要」「警告」「危险」等表述：

````mdx
# 转换前
**轮询的代价：**

- 每秒 N 次查询，数据库 CPU 飙升
- 99.99% 的查询都是「什么都没发生」

# 转换后
:::warning 轮询的代价
- 每秒 N 次查询，数据库 CPU 飙升
- 99.99% 的查询都是「什么都没发生」
:::
````

### 4.3 危险操作 → `:::danger`

涉及数据删除、不可逆操作：

````mdx
# 转换前
**删除 sincedb 文件**

sincedb_path => "/dev/null"

# 转换后
:::danger 慎用 /dev/null
生产环境中不要将 `sincedb_path` 设为 `/dev/null`，否则重启后会重复读取所有历史数据。仅限测试环境使用。
:::
````

### 4.4 扩展信息 → `:::note` 或 `:::info`

背景知识、补充说明、参考资料：

````mdx
# 转换前
**补充说明：**
binlog 有三种格式...

# 转换后
:::note binlog 的三种格式
- **STATEMENT**：记录 SQL 语句，节省空间
- **ROW**：记录每行数据变化，准确但量大
- **MIXED**：混用策略，默认选项
:::
````

---

## 五、标题层级与内容层次

### 5.1 数字前缀标题简化

**问题**：`## 1. 告警概述`、`### 1.1 什么是告警？` 中的 `1.` 和 `1.1` 前缀在 Rspress 侧边栏中会重复显示模块编号。

**转换规则**：

| 原标题 | 转换后 |
|---|---|
| `## 1. 告警概述` | `## 告警概述` |
| `### 1.1 什么是告警？` | `### 什么是告警？` |
| `### 2.3 条件配置详解` | `### 条件配置详解` |

**例外**：如果同一文档中某个标题与其他文档的标题高度重名（如「总结」「常见问题」），保留数字前缀以区分。

### 5.2 层级完整性检查

每篇文章应该有：

````
h1 (文章标题)
├── h2 (一级章节)  ← 至少 2 个
│   ├── h3 (二级章节)  ← 核心章节应有
│   └── h3
├── h2
└── h2 (总结/思考)
````

如果某篇文章只有 `h1` 和 `h2`，考虑拆分或增加 `h3` 子章节。

---

## 六、开场与结尾增强

### 6.1 开场检查

每篇文章的开场必须包含以下至少一种：

- **场景切入**：把读者拉进具体场景
- **痛点共鸣**：说出读者想说但没说出口的话
- **核心追问**：用问题撕开知识的面纱
- **反差对比**：打破固有认知

**缺失时的处理**：

````mdx
# 如果文章以「## Xxx 概述」或无钩子开头：
# 在 h1 标题后、第一个 h2 之前补充一段引导语

# 示例：
# 原开头：
## 1. 什么是告警？
告警是一种主动通知机制...

# 转换后：
## 什么是告警？

> 监控不仅仅是「看到」数据，更重要的是「知道」什么时候出了问题。
> Kibana 的告警功能让你能在指标异常时及时收到通知。

告警是一种**主动通知机制**，当监控指标满足预设条件时，系统自动发送通知。
````

### 6.2 结尾增强

如果文章没有结尾（或结尾只有简单的「总结」），增加：

1. **核心要点回顾**：`:::tip` 形式列出 3-5 个关键点
2. **留给你的问题**：以 `:::details` 或 `:::tip` 形式抛出 1-2 个思考题
3. **链接到下一节**：如果适用，给出下一节链接

````mdx
---

:::tip 核心要点回顾
1. **Canal 的核心原理**：伪装成 MySQL 从节点，接收 binlog 变更
2. **binlog 三种格式**：STATEMENT / ROW / MIXED
3. **Canal 架构**：Server + Instance + Client 三层设计
4. **与轮询的对比**：零轮询、实时推送、无需改业务代码
:::

:::details 留给你的问题
MySQL binlog 是顺序写入的，Canal 也是顺序接收的。
但在实际业务中，消费端可能处理不过来——这时候该怎么办？

提示：Canal 提供了 **Message ID 定位**和**时间戳定位**两种解决方案。
:::
````

---

## 七、内联样式规范化

### 7.1 `|...|` 管道标签 → `**...**` 加粗

原文中有 `|synchronized|`、`|HashMap|` 等样式：

````mdx
# 转换前
|synchronized| 的锁升级是一个**不可逆**的过程。
|HashMap| 不是线程安全的。

# 转换后
**synchronized** 的锁升级是一个**不可逆**的过程。
**HashMap** 不是线程安全的。
````

> **例外**：如果 `|...|` 样式用于标记同一段落中的多个关键词，且数量较多（≥3 个），保留 `|...|` 以保持视觉一致性。

### 7.2 `❌` / `✅` 前缀 → 容器或语义化

````mdx
# 转换前
```
❌ 危险写法：无限循环
while (true) {
    processData();
}

✅ 安全写法：设置超时
```

# 转换后
:::warning 危险写法：无限循环
```java
while (true) {
    processData();
}
```
:::

:::tip 安全写法：设置超时
```java
long startTime = System.currentTimeMillis();
while (true) {
    if (System.currentTimeMillis() - startTime > 60000) {
        throw new RuntimeException("执行超时");
    }
    if (!hasMoreData()) {
        break;
    }
    processData();
}
```
:::
````

---

## 八、表格规范化

### 8.1 表格格式检查

所有表格必须：

- 使用标准 Markdown 表格语法（`|---|---|`）
- 表头与内容行之间有分隔行
- 每行两端有 `|` 符号

### 8.2 转换「竖线文本图」为表格

原文中有类似以下格式：

````mdx
# 转换前（纯文本模拟的表格）
类型 | 说明 | 适用场景
-----|------|---------
Threshold | 阈值告警 | 指标超过/低于阈值
Inventory | 库存告警 | 资源状态变化

# 转换后（标准 Markdown 表格）
| 类型 | 说明 | 适用场景 |
|------|------|---------|
| Threshold | 阈值告警 | 指标超过/低于阈值 |
| Inventory | 库存告警 | 资源状态变化 |
````

---

## 九、常见问题块处理

### 9.1 Q&A 块 → `:::details` 容器

````mdx
# 转换前
### Q1：告警没有触发？

**答案**：
1. 检查规则是否启用
2. 检查时间范围是否包含数据
3. 检查索引模式是否匹配
4. 查看告警执行日志

### Q2：告警过多怎么办？

**答案**：
1. 调整阈值，降低敏感度
2. 增加 For 时间

# 转换后
:::details Q1：告警没有触发？
1. 检查规则是否启用
2. 检查时间范围是否包含数据
3. 检查索引模式是否匹配
4. 查看告警执行日志
:::

:::details Q2：告警过多怎么办？
1. 调整阈值，降低敏感度
2. 增加 For 时间（持续多久才告警）
3. 使用告警抑制
4. 按维度分组，减少重复告警
:::
````

---

## 十、图片与资源

### 10.1 图片路径规范

所有图片放在文档同级的 `assets` 目录下：

````mdx
# 正确
![告警配置界面](../assets/alert-config.png)

# 如果 assets 目录不存在，创建它并放入图片
# 路径：docs/middleware/kibana/assets/alert-config.png
````

### 10.2 图片缺失处理

如果文章引用了图片但文件不存在：

- 暂时注释掉图片引用，用 `:::note` 说明「配图待补充」
- 不删除引用，保留图片路径以便后续添加

---

## 十一、转换优先级

| 优先级 | 转换项 | 影响 |
|---|---|---|
| P0 | 代码块语言标识符修复 | 语法高亮、阅读体验 |
| P0 | ASCII 图 → `:::details` | 内容可读性 |
| P1 | 步骤列表 → `:::tip` 容器 | 层次感 |
| P1 | Q&A → `:::details` | 内容结构化 |
| P1 | 关键结论 → `:::tip` | 强调重点 |
| P2 | 开场引导语增强 | 读者留存 |
| P2 | 结尾增强（要点 + 思考题） | 完整性 |
| P2 | 标题数字前缀清理 | 侧边栏整洁 |
| P3 | `|...|` 管道标签规范化 | 风格一致性 |
| P3 | 表格格式修复 | 可维护性 |

---

## 十二、禁止事项

| 禁止行为 | 正确做法 |
|---|---|
| 删除实质内容 | 只调整表达方式，不删除信息 |
| 使用 emoji 代替语义标签 | 使用 `:::tip` / `:::warning` / `:::danger` |
| 过度使用 `:::details` | 每个 `:::details` 最多包裹 20 行；内容少时直接写 |
| 过度使用 `Badge` | 同一行不超过两个 Badge，同一章节不超过 5 个 |
| 添加无意义的空行 | 保持段落内句子紧凑，段间空一行 |
| 自行发明组件 | 只使用 Rspress 内置组件 |
