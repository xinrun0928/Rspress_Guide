# Rspress 容器规则

容器是标注重要信息的绝佳方式，用来提示读者注意关键内容。

---

## 两种语法风格

Rspress 支持两种容器语法：

| 语法风格 | 格式 |
|---|---|
| `:::` 语法 | `:::tip`、`:::warning` 等 |
| GitHub Alerts 语法 | `> [!TIP]`、`> [!WARNING]` 等 |

> **推荐使用 `:::` 语法**，因为 MDX 中使用 GitHub Alerts 语法在自定义标题时需要注意转义。

---

## `:::` 语法

### 可用类型

```mdx
:::note

这是 note 类型的容器

:::

:::tip

这是 tip 类型的容器

:::

:::info

这是 info 类型的容器

:::

:::warning

这是 warning 类型的容器

:::

:::danger

这是 danger 类型的容器

:::

:::details

这是 details 类型的容器

:::
```

### 自定义标题

**方式一（推荐）：** 直接在类型后加空格和标题

```mdx
:::tip 自定义标题

这是一个带有自定义标题的容器

:::
```

**方式二：** 使用大括号语法（需注意 MDX 转义）

```mdx
:::tip\{title="自定义标题"}

这是一个带有自定义标题的容器

:::
```

---

## GitHub Markdown Alerts 语法

```mdx
> [!NOTE]
> 这是一个 note 类型的 block

> [!TIP]
> 这是一个 tip 类型的 block

> [!INFO]
> 这是一个 info 类型的 block

> [!WARNING]
> 这是一个 warning 类型的 block

> [!DANGER]
> 这是一个 danger 类型的 block

> [!DETAILS]
> 这是一个 details 类型的 block
```

---

### 容器间距规范

容器标记 `:::` 前后必须保留空行，容器内容与闭合标记 `:::` 上下各空一行：

```mdx
这是正文内容。

:::tip 提示标题

这里是提示内容，与前后正文各隔开一行。

:::

正文继续。
```

:::warning 常见错误
以下写法是错误的，缺少必要的空行：

```mdx
这是正文。
:::tip
内容贴得太近
:::
正文又贴在一起了。
```
:::

---

## 使用规范

| 规范 | 说明 |
|---|---|
| 类型必须小写 | 使用 `:::tip`、`:::warning`，大写如 `:::Tip` 不会被识别 |
| MDX 中的大括号语法需转义 | 在 `.mdx` 文件中，`{` 和 `}` 在某些场景下需用 `\{` 和 `\}` 转义 |
| 推荐 `::: 标题` 语法 | 避免 MDX 转义问题的最简方式 |
| 禁止使用 emoji 代替 | 使用内置容器类型，**禁止**用 emoji 如 `⚠️`、`❗` 等作为图标 |
| 容器前后必须空行 | `:::` 闭合标记上方、容器标记下方各保留一个空行，内容与 `:::` 闭合之间也各空一行 |

### 类型与语义对照

| 类型 | 语义场景 |
|---|---|
| `note` | 补充说明、背景信息 |
| `tip` | 实用技巧、建议、最佳实践 |
| `info` | 额外信息、参考资料 |
| `warning` | 需要注意的潜在问题 |
| `danger` | 危险操作、不可逆后果、破坏性操作 |
| `details` | 可展开的详细信息 |

---

## 常见问题

**`:::warning` 和 `:::danger` 的区别是什么？**

- `warning`：提醒读者注意可能的风险或常见的错误做法，但不会造成严重后果
- `danger`：警示破坏性操作，如删除数据、不可逆的配置修改等，可能导致系统故障或数据丢失

**details 容器有什么特殊用法？**

```mdx
:::details 点击展开查看更多

这里是折叠的内容，可以包含多行、代码块等丰富内容。

:::
```

`<details>` 标签支持嵌套 markdown 内容，适合长代码、完整配置等。
