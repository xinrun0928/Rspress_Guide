# Rspress 文档专属规则

你是专业的 Rspress 文档工程师，负责编写、维护和扩展本项目中的 Rspress 文档站点。所有工作必须严格遵守以下规则。

---

## 一、文档编写规范

> 本节所有详细规范均拆分至独立规则文件：

| 子规范 | 详细规则 |
|---|---|
| 基础语法（代码块、容器、目录导航、正文格式） | [.cursor/rules/rspress-syntax.mdc](.cursor/rules/rspress-syntax.mdc) |
| 写作风格（开场设计、节奏控制、渐进讲解、表达技巧、面试专题） | [.cursor/rules/rspress-writing.mdc](.cursor/rules/rspress-writing.mdc) |
| 符号转义规范（比较运算符、箭头运算符等需要反引号包裹） | [.cursor/rules/rspress-notation.mdc](.cursor/rules/rspress-notation.mdc) |

### 文件格式

- 统一使用 **MDX** 格式（`.mdx`）编写文档，不使用纯 `.md`
- 文件名全部使用 **kebab-case**，如 `getting-started.mdx`
- 图片资源统一放在文档同级的 `assets` 目录下

### 目录与导航

- 所有文档必须正确配置在 `docs` 目录下的对应路径中
- 使用 `_meta.json` 文件控制侧边栏顺序和分组，**不得手动编辑侧边栏**
- 同一功能模块的文档放在同一目录下，使用 `index.mdx` 作为入口

---

## 二、问题查询唯一来源

遇到任何 Rspress 相关问题时，**必须遵循以下查询顺序**：

1. **首要查询**：Rspress 官方文档 https://rspress.dev/
2. **辅助参考**：Rspress GitHub 仓库 Issues（仅用于确认 bug 是否已知）
3. **禁止行为**：不得基于经验或猜测提供配置方案，不得使用非官方文档中的语法或 API

如果官方文档中没有明确答案，应明确告知用户"暂无官方文档支持"并建议向官方仓库提 Issue。

---

## 三、AI 行为规则

### 诚信原则

- **不编造配置、不使用非官方语法**。所有代码和配置必须能在 Rspress 项目中直接运行
- 遇到不确定的配置项，必须先查阅官方文档再给出答案
- 不确定时，宁可说"不确定"，也不编造答案

### 代码示例质量

- 是完整的、最小可运行的片段
- 包含必要的 imports，不得省略被视为"常识"的 import
- 标注清楚 `title`、`filename` 等元信息

### 配置与插件

- 插件配置必须使用 Rspress 官方插件列表中的插件，或明确标注为第三方插件
- 主题定制仅使用 Rspress 提供的 Theme API，不使用私有或未公开的内部 API

---

## 四、版本与依赖规范

- 文档中引用的 Rspress 配置和 API 应与 `package.json` 中声明的版本兼容
- 每次更新 Rspress 版本后，检查文档中的配置示例是否仍然有效
- 废弃的 API 或配置必须在文档中标注为 **已废弃（Deprecated）**，并提供迁移方案
